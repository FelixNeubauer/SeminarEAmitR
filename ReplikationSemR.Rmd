---
title: "Subways and Road Congestion"
subtitle: "Seminar Empirische Analysen mit R"
author: "Felix Neubauer"

params:
  viridis_palette: viridis

output:
  prettydoc::html_pretty:
    theme: cayman
    toc: yes
    keep_md: true
editor_options: 
  markdown: 
    wrap: sentence
---

```{r setup, include=FALSE}
# Globale Chunk-Optionen setzen
knitr::opts_chunk$set(echo = FALSE, warnings = FALSE, messages = FALSE)

# Ganze Zahlen im deutschen Format ausgeben
format_int <- function(x) {
  format(x, big.mark = ".", decimal.mark = ",", scientific = FALSE, trim = TRUE)
}

# Dezimalzahlen runden und formatiert ausgeben
format_num <- function(x, digits = 2) {
  format(round(x, digits), big.mark = ".", decimal.mark = ",", scientific = FALSE, trim = TRUE)
}

# Minimal- und Maximalwert als Spannweite ausgeben
format_range <- function(x) {
  x <- x[!is.na(x)]

  if (length(x) == 0) {
    return("--")
  }

  paste0(format(min(x), scientific = FALSE, trim = TRUE), "--", format(max(x), scientific = FALSE, trim = TRUE))
}

# Unterschiedliche Werte einer vorhandenen Variable zählen
count_distinct <- function(data, variable) {
  if (!variable %in% names(data)) {
    return("--")
  }

  format_int(dplyr::n_distinct(data[[variable]], na.rm = TRUE))
}

# Spannweite einer vorhandenen Variable berechnen
range_variable <- function(data, variable) {
  if (!variable %in% names(data)) {
    return("--")
  }

  format_range(data[[variable]])
}

# Anteil als Prozentwert ausgeben
share_percent <- function(x) {
  format_num(mean(x, na.rm = TRUE) * 100, 1)
}
```

```{r libraries, include=FALSE}
# Benötigte Pakete laden
library(haven)
library(dplyr)
library(ggplot2)
library(tidyr)
library(grid)
library(lubridate)
library(knitr)
library(kableExtra)
library(fixest)
library(broom)
library(purrr)
library(patchwork)
library(readr)
```

```{r datainput, include=FALSE}
# Rohdaten aus dem Data-Ordner einlesen
UrbanTransitReports_input <- read_dta("Data/UrbanTransitReports.dta")
SubwayLines_input <- read_dta("Data/SubwayLines.dta")
data_table1_input <- read_dta("Data/data_table1.dta")
CityChars_input <- read_dta("Data/CityChars.dta")
BaseSamp_input <- read_dta("Data/BaseSamp.dta")
ExtendSample_input <- read_dta("Data/ExtendSample.dta")
WithAgainstTraffic_input <- read_dta("Data/WithAgainstTraffic.dta")
RushNonrusHours_input <- read_dta("Data/RushNonrusHours.dta")
```


# 0 Überblick

### Zusammenfassung

In ihrem Paper *Subways and Road Congestion* untersuchen Yizhen Gu, Chang Jiang, Junfu Zhang und Ben Zou, ob der Ausbau von U-Bahn-Systemen tatsächlich dazu beitragen kann, Straßenverkehr in chinesischen Städten zu entlasten. Die Studie setzt dabei an einem zentralen verkehrsökonomischen Problem an: Viele schnell wachsende Städte leiden unter zunehmendem Stau, gleichzeitig werden U-Bahnen häufig als Lösung zur Reduktion von Verkehrsbelastung betrachtet.

Besonders relevant ist der chinesische Kontext, da China in den vergangenen Jahren einen massiven Ausbau städtischer U-Bahn-Netze erlebt hat. Zwischen 2001 und 2017 stieg die Gesamtlänge der U-Bahn-Linien in China stark an, während gleichzeitig Bevölkerung, Autobesitz und Verkehrsaufkommen in vielen Großstädten zunahmen. Dadurch entsteht ein geeigneter empirischer Rahmen, um den Effekt neuer U-Bahn-Linien auf den Straßenverkehr zu untersuchen.

Für ihre Analyse nutzen die Autoren Daten zu 45 neu eröffneten U-Bahn-Linien in 25 chinesischen Städten im Zeitraum von August 2016 bis Dezember 2017. Diese Informationen kombinieren sie mit hochfrequenten Verkehrsdaten von Baidu Maps, die stündliche Geschwindigkeitsmessungen auf Ebene einzelner Straßenabschnitte enthalten. Dadurch können die Autoren sehr genau beobachten, wie sich die Fahrgeschwindigkeit auf Straßen in der Nähe neuer U-Bahn-Linien vor und nach deren Eröffnung verändert.

Methodisch basiert die Studie auf einem Difference-in-Differences-Ansatz. Dabei werden Straßenabschnitte, die direkt von einer neuen U-Bahn-Linie betroffen sein könnten, mit Kontrollstraßen in Städten verglichen, in denen im Untersuchungszeitraum keine neue U-Bahn-Linie eröffnet wurde. Ziel ist es, den kausalen Effekt der U-Bahn-Eröffnung auf die Verkehrsgeschwindigkeit zu isolieren und allgemeine zeitliche Entwicklungen oder saisonale Muster möglichst herauszurechnen.

##### Zentrale Ergebnisse

Die Autoren kommen zu dem Ergebnis, dass neue U-Bahn-Linien den Straßenverkehr zumindest kurzfristig messbar entlasten. Im ersten Jahr nach der Eröffnung steigt die Geschwindigkeit auf direkt betroffenen Straßen während der Stoßzeiten um durchschnittlich etwa vier Prozent. Der Effekt ist besonders ausgeprägt auf zuvor stark überlasteten Straßen und nimmt mit zunehmender Entfernung zur neuen U-Bahn-Linie ab. Ergänzende Analysen aus Beijing deuten außerdem darauf hin, dass eine bessere U-Bahn-Anbindung mit mehr U-Bahn-Fahrten sowie weniger Bus- und Autofahrten verbunden ist.

### Replikationsumfang

Der Replikationsumfang umfasst einen großen Teil der zentralen empirischen Analysen des Papers. Repliziert wurden sowohl die deskriptiven Grundlagen der Studie als auch das Hauptergebnis, die dynamischen Effekte, mehrere Robustheits- und Timing-Checks sowie die heterogenen Effekte nach Straßenmerkmalen. Table 1 wurde nicht als empirische Tabelle repliziert, sondern als eigene Grafik auf Basis der tabellarischen Linienauflistung visualisiert, um das Treatment Timing und das CNY-Problem anschaulicher darzustellen. Table 6 konnte dagegen nicht repliziert werden, weil die dafür benötigten Individual- und Haushaltsdaten nicht im Replikationspaket enthalten waren. Figure 7 wurde ohne Datengrundlage nachgebildet, da es sich um eine schematische Darstellung der ökonomischen Interpretation handelt.

| Element  | Status                       | Rolle im Report                                                                                        |
| -------- | ---------------------------- | ------------------------------------------------------------------------------------------------------ |
| Figure 1 | repliziert                   | Kontext: chinesischer U-Bahn-Boom                                                                      |
| Figure 2 | repliziert                   | Dynamisches Hauptergebnis / Event-Study-Darstellung                                                    |
| Figure 3 | repliziert                   | Placebo Timing / Robustheit                                                                            |
| Figure 4 | repliziert                   | Längerer Pre-Treatment-Zeitraum / Pre-Trend-Robustheit                                                 |
| Figure 5 | repliziert                   | Timing-Robustheit / CNY-Problematik                                                                    |
| Figure 6 | repliziert                   | Heterogene Effekte / Mechanismus und Robustheit                                                        |
| Figure 7 | ohne Daten repliziert        | Schematische ökonomische Interpretation                                                                |
| Table 1  | Grafik basierend auf Tabelle | Treatment Timing und CNY-Problem                                                                       |
| Table 2  | repliziert                   | Deskriptive Unterschiede zwischen Treatment und Control                                                |
| Table 3  | repliziert                   | Statisches Hauptergebnis / Baseline-Schätzung                                                          |
| Table 4  | repliziert                   | Robustheit gegenüber längeren Pre-Periods und Discontinuity-Spezifikationen                            |
| Table 5  | repliziert                   | Subsamples nach Opening-Zeitpunkt / Timing-Robustheit                                                  |
| Table 6  | nicht repliziert             | Substitution zwischen Verkehrsmitteln; nicht repliziert, da die benötigten Daten nicht vorhanden waren |




# 1 Kontext

### Warum ist Road Congestion ein ökonomisches Problem?

Straßenstau ist aus ökonomischer Sicht mehr als nur ein alltägliches Mobilitätsproblem. Er verursacht Zeitverluste für Pendlerinnen und Pendler, senkt die Zuverlässigkeit von Fahrten und bindet Ressourcen, die produktiver eingesetzt werden könnten. Besonders relevant ist dabei, dass einzelne Verkehrsteilnehmer die von ihnen verursachten Verzögerungen für andere nicht vollständig berücksichtigen. Stau ist damit ein klassisches Externalitätenproblem: Jede zusätzliche Autofahrt kann die Geschwindigkeit vieler anderer Verkehrsteilnehmer verringern. Gu et al. (2021) zeigen diesen Zusammenhang auch in ihrer Wohlfahrtsbetrachtung. Wenn eine neue U-Bahn-Linie die Straßengeschwindigkeit erhöht, profitieren nicht nur die Personen, die zur U-Bahn wechseln, sondern auch jene, die weiterhin Auto oder Bus fahren. Für Peking berechnen die Autoren, dass allein die Zeitersparnis durch höhere Straßengeschwindigkeit pro Auto- oder Buspendelfahrt etwa 0,10 US-Dollar wert ist.


### Fundamental Law of Road Congestion

Das „Fundamental Law of Road Congestion“ beschreibt die Idee, dass zusätzlicher Straßenraum langfristig nicht unbedingt zu weniger Stau führt. Nach dieser Logik steigt die gefahrene Verkehrsmenge ungefähr proportional mit der verfügbaren Straßenkapazität: Werden mehr Straßen oder Fahrspuren gebaut, wird Autofahren attraktiver, wodurch zusätzlicher Verkehr induziert wird. Duranton und Turner (2011) finden für US-Städte, dass dieses Muster auch für städtische Straßen gilt und dass öffentlicher Verkehr dort die insgesamt gefahrenen Fahrzeugkilometer nicht wesentlich reduziert. Für das Paper von Gu et al. (2021) ist diese Literatur der zentrale Ausgangspunkt: Wenn zusätzliche Straßenkapazität Stau kaum reduziert, stellt sich die Frage, ob U-Bahnen als alternative Form urbaner Verkehrsinfrastruktur anders wirken. Die Studie liefert hierfür Gegenbelege im chinesischen Kontext, zumindest kurzfristig: Nach der Eröffnung neuer U-Bahn-Linien steigt die Geschwindigkeit auf nahegelegenen Straßen messbar an.

### Warum China ein geeignetes Setting ist

China ist für diese Forschungsfrage besonders geeignet, weil dort in kurzer Zeit ein außergewöhnlich starker Ausbau urbaner U-Bahn-Systeme stattgefunden hat. Während 2001 nur vier Städte in Festlandchina ein U-Bahn-System hatten, verfügten Ende 2017 bereits 30 Städte über insgesamt 4.476 Kilometer U-Bahn-Linien; zusätzlich hatten zwölf weitere Städte ihre ersten Linien im Bau. Diese starke Expansion erzeugt viele klar datierbare Treatment-Zeitpunkte. Gu et al. (2021) nutzen konkret 45 neue U-Bahn-Linien, die zwischen August 2016 und Dezember 2017 in 25 Städten eröffnet wurden. Gleichzeitig stehen mit Baidu-Maps-Daten sehr detaillierte Informationen zur Straßengeschwindigkeit auf Ebene einzelner Straßenabschnitte zur Verfügung. Die Kombination aus massivem U-Bahn-Ausbau und granularen Echtzeit-Verkehrsdaten macht China daher zu einem ungewöhnlich guten Setting, um den Effekt neuer U-Bahn-Linien auf Straßenstau empirisch zu untersuchen.

##### Figure 1
```{r DataUrbantransitVorbereiten, echo=FALSE, include = FALSE}
# Datensatz einlesen
urban <- UrbanTransitReports_input

# Spaltennamen übersetzen
urban_en <- urban %>%
  rename(
    year_rel = year,
    city_id = id,
    gdp = GDP
  ) %>%
  rename(
    city = `城市`,
    year = `年份`,
    pop_10k = `年末总人口万`,
    city_district_pop_10k = `市辖区年末总人口万`,
    n_bus = `年末实有公共汽电车营运车辆数辆`,
    bus_mileage_10k_km = `公共汽电车运营里程万公里`,
    bus_ridership_10k = `公共汽电车客运量万人次`,
    n_taxi = `出租汽车车辆数运营车数合计辆`,
    taxi_mileage_10k_km = `出租汽车运营里程万公里`,
    taxi_trips_10k = `出租汽车载客车次总数万车次`,
    taxi_ridership_10k = `出租汽车客运量万人次`,
    n_subway_vehicle = `轨道交通运营车数辆`,
    n_subway_lines = `轨道交通运营线路条数合计条`,
    subway_length_total_km = `轨道交通运营线路总长度公里`,
    subway_length_km = `轨道交通运营线路长度公里`,
    subway_mileage_10k_train_km = `轨道交通运营里程万列公里`,
    subway_ridership_10k = `轨道交通客运量万人次`,
    road_surface_10k_sqm = `年末实有城市道路面积万平方米`,
    road_surface_per_capita_sqm = `人均城市道路面积平方米`,
    n_civilian_cars_10k = `民用汽车拥有量万辆`,
    n_private_cars_10k = `私人汽车拥有量万辆`,
    district_gdp_per_capita_yuan = `人均地区生产总值元`,
    urban_disposable_income_per_capita_yuan = `城镇居民人均可支配收入元`,
    urban_consumption_expenditure_per_capita_yuan = `城镇居民人均生活消费性支出元`,
    subway_open_year = `地铁开通年份`,
    has_subway = `是否开通地铁`,
    city_gdp_per_capita_yuan = `城市人均GDP`
  )
# Alle Städtenamen für Übersetzung raussuchen
unique(urban_en$city)

# Neue Spalte mit übersetzten Städtenamen und Reihenfolge für Übersichtlichkeit anpassen
urban_en <- urban_en %>%
  mutate(
    city_en = recode(
      city,
      "上海" = "Shanghai",
      "北京" = "Beijing",
      "南京" = "Nanjing",
      "南宁" = "Nanning",
      "南昌" = "Nanchang",
      "厦门" = "Xiamen",
      "合肥" = "Hefei",
      "哈尔滨" = "Harbin",
      "大连" = "Dalian",
      "天津" = "Tianjin",
      "广州" = "Guangzhou",
      "成都" = "Chengdu",
      "昆明" = "Kunming",
      "杭州" = "Hangzhou",
      "武汉" = "Wuhan",
      "沈阳" = "Shenyang",
      "深圳" = "Shenzhen",
      "石家庄" = "Shijiazhuang",
      "福州" = "Fuzhou",
      "西安" = "Xi'an",
      "贵阳" = "Guiyang",
      "郑州" = "Zhengzhou",
      "重庆" = "Chongqing",
      "长春" = "Changchun",
      "长沙" = "Changsha"
    )
  ) %>%
  relocate(city_en, .after = city)

urban_en <- urban_en %>%
  relocate(city_id, .before = city)

```

```{r DataSubwayLines, include= FALSE}
# Datensatz laden
lines <- SubwayLines_input

# Wie im Original Hong Kong entfernen
lines_nohk <- lines %>%
  filter(city != "Hong Kong")

lines_nohk %>%
  count(city, sort = TRUE)
```


```{r Fig1Vorbereiten, echo=FALSE}
# Datensatz urban_en für Fig 1, zuerst irrelevante Spalten entfernen

urban_fig1 <- urban_en %>%
  select(city, city_en, year, subway_length_total_km, subway_ridership_10k)


#  Aggregieren auf Jahresebene, Pendant zu diesem Stata Befehl: collapse (sum) subway_length subway_ridership, by(year)

annual_temp <- urban_fig1 %>%
  group_by(year) %>%
  summarise(
    subway_length = sum(subway_length_total_km, na.rm = TRUE),
    subway_ridership = sum(subway_ridership_10k, na.rm = TRUE),
    .groups = "drop"
  )


# Im Vorbild ist die Ridership in Millionen angegeben, deswegen müssen die Werte umgerechnet werden

annual_temp <- annual_temp %>%
  mutate(
    subway_ridership = subway_ridership / 100
  )


# Jahr 2009 entfernen, da es keine Ridership enthält, Datenfehler?

annual_temp <- annual_temp %>%
  filter(year != 2009)




# Restliche Daten aus SubwayLines.dta nutzen
# Neu hinzugekommene Streckenlänge je Eröffnungsjahr aufsummieren

lines_year <- lines_nohk %>%
  group_by(openyear) %>%
  summarise(
    lengthkm = sum(lengthkm, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  arrange(openyear)

# Laufende kumulative Summe als tatsächliche Länge je Jahr bilden
lines_year <- lines_year %>%
  mutate(
    subway_length2 = cumsum(lengthkm)
  )


# Für den Merge openyear in year umbenennen

lines_year <- lines_year %>%
  rename(year = openyear) %>%
  select(year, subway_length2)


```

```{r merge fig1, echo=FALSE}
# Die beiden vorbereiteten Datensätze werden zusammengefügt, full join, damit keine Daten verloren gehen

fig1_df <- full_join(lines_year, annual_temp, by = "year") %>%
  arrange(year)


# NA Werte statt 0 wie im Stata Code

fig1_df <- fig1_df %>%
  mutate(
    subway_ridership = na_if(subway_ridership, 0),
    subway_length = na_if(subway_length, 0)
  )


# Labels von Vorbild Grafik übernehmen

fig1_df <- fig1_df %>%
  mutate(
    NCities = case_when(
      year == 2000 ~ "4 cities",
      year == 2017 ~ "30 cities",
      TRUE ~ NA_character_
    )
  )


# Wie in originaler Grafik Begrenzung auf Jahre ab 2000
plot_df <- fig1_df %>%
  filter(year >= 2000, year <= 2017)

```

```{r plotfig1, echo=FALSE, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 1 aus Gu et. al (2021)'}
# Skalierungsfaktor für die rechte Achse festlegen
scale_factor <- 3.2


# Plot erstellen

p_fig1 <- ggplot(plot_df, aes(x = year)) +
  geom_line(
    # Graph Ridership
    data = plot_df %>% filter(year >= 2002, !is.na(subway_ridership)),
    aes(y = subway_ridership, color = "ridership"),
    linewidth = 1.1,
    lineend = "round"
  ) +
  geom_line(
    # Graph Length
    data = plot_df %>% filter(year >= 2002, !is.na(subway_length2)),
    aes(y = subway_length2 * scale_factor, color = "length"),
    linewidth = 1.1,
    linetype = "31"
  ) +
  # Achsen und Skalen konfigurieren und Skalenstriche hinzufügen
  scale_x_continuous(
    breaks = c(2000, 2005, 2010, 2015, 2020),
    limits = c(2000, 2020)
  ) +
  scale_y_continuous(
    name = "total ridership (million)",
    breaks = c(0, 5000, 10000, 15000),
    limits = c(0, 16000),
    sec.axis = sec_axis(
      ~ . / scale_factor,
      name = "total length (km)",
      breaks = c(0, 1000, 2000, 3000, 4000, 5000)
    )
  ) +
  # Farben mit Hexpicker aus Original übernommen
  scale_color_manual(
    values = c("ridership" = "#6d8d84", "length" = "#90343a"),
    breaks = c("ridership", "length")
  ) +
  # X-Achse beschriften
  labs(
    x = "year",
    color = NULL
  ) +
  # Labels aus Original für Anzahl der Städte 2002 und 2017
  geom_text(
    data = tibble(
      x = c(2000.2, 2017.5),
      y = c(236 * scale_factor + 100, 4476 * scale_factor + 100),
      label = c("4 cities", "30 cities")
    ),
    aes(x = x, y = y, label = label),
    inherit.aes = FALSE,
    color = "black",
    size = 2.8,
    hjust = 0,
    vjust = c(-0.1, -0.15)
  ) +
  # Legende hinzugefügt
  guides(
    color = guide_legend(
      nrow = 1,
      byrow = TRUE,
      override.aes = list(
        linewidth = c(1.3, 1.3),
        linetype = c("solid", "dashed")
      )
    )
  ) +
  # Theme mit Farben, Positionen und vertikalen Skalenbeschriftungen
  theme_classic(base_size = 12) +
  theme(
    legend.position = "bottom",
    legend.direction = "horizontal",
    legend.key.width = unit(2.4, "cm"),
    legend.key.height = unit(0.6, "cm"),
    legend.background = element_rect(color = "black", fill = "white", linewidth = 0.4),
    legend.box.background = element_rect(color = "black", fill = "white", linewidth = 0.4),
    legend.margin = margin(6, 10, 6, 10),
  
    panel.border = element_blank(),
    panel.grid.major.y = element_line(color = "grey90", linewidth = 0.3),
    panel.grid.minor = element_blank(),
  
    axis.line.x.bottom = element_line(color = "black"),
    axis.line.y.left = element_line(color = "black"),
    axis.line.y.right = element_line(color = "black"),
    axis.line.x.top = element_blank(),
    
    axis.text.y = element_text(angle = 90, vjust = 0.5, hjust = 0.5),
    axis.text.y.right = element_text(angle = 90, vjust = 0.5, hjust = 0.5),
  
    axis.title.y.right = element_text(
      angle = 90,
      margin = margin(l = 12)
    ),
    # Aspect Ratio vom Original übernommen
    aspect.ratio = 0.674
  )

p_fig1
```

```{r plotfig1Figma, echo=FALSE, include=FALSE, dpi=300}
# Skalierungsfaktor für die rechte Achse festlegen
scale_factor <- 3.2


# Plot erstellen

p_fig1_figma <- ggplot(plot_df, aes(x = year)) +
  geom_line(
    # Graph Ridership
    data = plot_df %>% filter(year >= 2002, !is.na(subway_ridership)),
    aes(y = subway_ridership, color = "ridership"),
    linewidth = 1.1,
    lineend = "round"
  ) +
  geom_line(
    # Graph Length
    data = plot_df %>% filter(year >= 2002, !is.na(subway_length2)),
    aes(y = subway_length2 * scale_factor, color = "length"),
    linewidth = 1.1,
    linetype = "31"
  ) +
  # Achsen und Skalen konfigurieren und Skalenstriche hinzufügen
  scale_x_continuous(
    breaks = c(2000, 2005, 2010, 2015, 2020),
    limits = c(2000, 2020)
  ) +
  scale_y_continuous(
    name = "total ridership (million)",
    breaks = c(0, 5000, 10000, 15000),
    limits = c(0, 16000),
    sec.axis = sec_axis(
      ~ . / scale_factor,
      name = "total length (km)",
      breaks = c(0, 1000, 2000, 3000, 4000, 5000)
    )
  ) +
  # Farben für Präsentation angepasst
  scale_color_manual(
    values = c("ridership" = "#8BAFAC", "length" = "#171738"),
    breaks = c("ridership", "length")
  ) +
  # X-Achse beschriften
  labs(
    x = "year",
    color = NULL
  ) +
  # Labels aus Original für Anzahl der Städte 2002 und 2017
  geom_text(
    data = tibble(
      x = c(2000.2, 2017.5),
      y = c(236 * scale_factor + 100, 4476 * scale_factor + 100),
      label = c("4 cities", "30 cities")
    ),
    aes(x = x, y = y, label = label),
    inherit.aes = FALSE,
    color = "black",
    size = 2.8,
    hjust = 0,
    vjust = c(-0.1, -0.15)
  ) +
  # Legende hinzugefügt
  guides(
    color = guide_legend(
      nrow = 1,
      byrow = TRUE,
      override.aes = list(
        linewidth = c(1.3, 1.3),
        linetype = c("solid", "dashed")
      )
    )
  ) +
  # Theme mit Farben, Positionen und vertikalen Skalenbeschriftungen
  theme_classic(base_size = 12) +
  theme(
    legend.position = "bottom",
    legend.direction = "horizontal",
    legend.key.width = unit(2.4, "cm"),
    legend.key.height = unit(0.6, "cm"),
    legend.background = element_rect(color = "black", fill = "white", linewidth = 0.4),
    legend.box.background = element_rect(color = "black", fill = "white", linewidth = 0.4),
    legend.margin = margin(6, 10, 6, 10),
  
    panel.border = element_blank(),
    panel.grid.major.y = element_line(color = "grey90", linewidth = 0.3),
    panel.grid.minor = element_blank(),
  
    axis.line.x.bottom = element_line(color = "black"),
    axis.line.y.left = element_line(color = "black"),
    axis.line.y.right = element_line(color = "black"),
    axis.line.x.top = element_blank(),
    
    axis.text.y = element_text(angle = 90, vjust = 0.5, hjust = 0.5),
    axis.text.y.right = element_text(angle = 90, vjust = 0.5, hjust = 0.5),
  
    axis.title.y.right = element_text(
      angle = 90,
      margin = margin(l = 12)
    ),
    # Aspect Ratio vom Original übernommen
    aspect.ratio = 1.15
  )

p_fig1_figma
```

```{r fig1_save, include=FALSE}
# Grafik als PNG speichern
ggsave(
  filename = "FertigeGrafiken/Fig1_R.png",
  plot = p_fig1
)


```

```{r fig1_save_figma, include=FALSE}
# Grafik als PNG speichern
ggsave(
  filename = "FertigeGrafiken/Fig1_R_Figma.png",
  plot = p_fig1_figma
)


```





# 2 Institutioneller Hintergrund und Treatment

Die Studie nutzt den massiven Ausbau chinesischer U-Bahn-Systeme als empirisches Setting, um den Effekt neuer Schieneninfrastruktur auf Straßenverkehr zu untersuchen. Institutionell relevant ist dabei, dass viele chinesische Städte in kurzer Zeit neue U-Bahn-Linien eröffneten und diese Eröffnungen klar datierbare Ereignisse darstellen. Dadurch lassen sich die neuen Linien als Treatment interpretieren, dessen Wirkung auf die Straßengeschwindigkeit vor und nach der Eröffnung analysiert werden kann.


### Neue U-Bahn-Linien als Treatment

Das Treatment in der Studie besteht in der Eröffnung neuer U-Bahn-Linien in chinesischen Städten. Gu et al. (2021) untersuchen insgesamt 45 neue Linien beziehungsweise Linienerweiterungen, die zwischen August 2016 und Dezember 2017 in 25 Städten eröffnet wurden. Die Grundidee ist, dass eine neue U-Bahn-Linie für bestimmte Fahrten eine Alternative zum Auto oder Bus schafft und dadurch die Nachfrage nach Straßennutzung auf nahegelegenen oder substituierbaren Straßenabschnitten sinken kann. Besonders relevant sind dabei nicht einfach alle Straßen in der Nähe einer U-Bahn, sondern jene Straßenabschnitte, die laut Routenplanungsdaten von Baidu Maps tatsächlich als Autostrecken für Fahrten infrage kommen, die alternativ mit der neuen U-Bahn zurückgelegt werden könnten. Diese direkt betroffenen Straßenabschnitte bilden den Kern des Treatment-Samples. Der kausale Effekt wird somit als Veränderung der Straßengeschwindigkeit nach der Eröffnung einer neuen U-Bahn-Linie interpretiert, relativ zu vergleichbaren Straßenabschnitten in Kontrollstädten ohne neue Linie im selben Zeitraum.

### Treatment Timing und Chinese New Year

Ein zentrales Identifikationsproblem der Studie liegt im Timing der U-Bahn-Eröffnungen. Die offiziellen Eröffnungsdaten sind nicht zufällig verteilt, sondern häufen sich stark am Jahresende: 25 der 45 untersuchten Linien wurden im Dezember eröffnet. Das ist problematisch, weil diese Phase zeitlich nahe am Chinese New Year liegt, einer Periode, in der sich Verkehrsmuster in chinesischen Städten ohnehin stark verändern können. Viele Menschen reisen aus den großen Städten ab, wirtschaftliche Aktivität verlangsamt sich und Straßen können auch ohne neue U-Bahn-Linie weniger ausgelastet sein. Dadurch könnte ein beobachteter Anstieg der Straßengeschwindigkeit fälschlicherweise dem Treatment zugeschrieben werden, obwohl er teilweise durch saisonale Effekte entsteht. Dieses Problem wird zusätzlich dadurch verschärft, dass die Kontrollstädte im Durchschnitt kleiner und etwas ärmer sind als die Treatment-Städte, sodass sich saisonale Verkehrsrückgänge zwischen beiden Gruppen unterscheiden können. Die Autoren adressieren dieses Problem, indem sie in ihrem Difference-in-Differences-Design für unterschiedliche Saisonalität nach Stadtmerkmalen kontrollieren und zusätzliche Robustheitschecks mit Linien durchführen, die außerhalb dieser kritischen Jahresendperiode eröffnet wurden.

##### Table 1 Grafik

```{r grafik1_daten}
# Daten aus Table 1 für die Monatsgrafik vorbereiten
data_grafik1_monat <- data_table1_input %>%
  mutate(
    panel = factor(
      panel,
      levels = c("Panel A", "Panel B", "Panel C")
    ),
    
    opening_year = as.integer(opening_year),
    
    opening_month = if_else(
      date_precision == "day",
      as.integer(month(opening_date)),
      NA_integer_
    ),
    
    # Dezember bekommt deutlich mehr Abstand zu November und zu den
    # reinen Jahresangaben rechts daneben. So bleibt genug Platz für
    # breiter geclusterte Dezember-Punkte.
    x_plot = case_when(
      date_precision == "year" ~ 15.4,
      opening_month == 12L ~ 13.0,
      TRUE ~ as.numeric(opening_month)
    ),
    
    line_status = case_when(
      panel == "Panel A" ~ "Newly opened lines\nAug 2016–Dec 2017",
      panel %in% c("Panel B", "Panel C") & opening_year <= 2017 ~ "Existing lines\nopening year ≤ 2017",
      panel %in% c("Panel B", "Panel C") & opening_year >= 2018 ~ "Planned lines\nopening year ≥ 2018",
      TRUE ~ NA_character_
    ),
    
    line_status = factor(
      line_status,
      levels = c(
        "Newly opened lines\nAug 2016–Dec 2017",
        "Existing lines\nopening year ≤ 2017",
        "Planned lines\nopening year ≥ 2018"
      )
    )
  ) %>%
  filter(!is.na(x_plot), !is.na(line_status))

# Die y-Achse wird komprimiert: Der Ausreißer 1964 bleibt sichtbar,
# aber die leeren Jahre bis 2004 nehmen nur wenig Platz ein. Danach
# werden alle Jahre einheitlich auf der y-Achse verteilt.
map_year_grafik1 <- function(year) {
  case_when(
    year < 2004 ~ 0,
    TRUE ~ -(year - 2004 + 2)
  )
}

# Punkte mit identischem Monat und Jahr werden horizontal nebeneinander
# angeordnet. So bleiben Mehrfacheröffnungen panelübergreifend sichtbar,
# ohne dass sie sich überdecken. Die Breite eines Clusters hängt dabei
# von der Anzahl an Eröffnungen im Monat ab.
data_grafik1_monat_cluster <- data_grafik1_monat %>%
  group_by(opening_year, x_plot) %>%
  arrange(panel, line_status, opening_date, .by_group = TRUE) %>%
  mutate(
    n_cluster = n(),
    cluster_id = row_number(),
    cluster_span = case_when(
      is.na(first(opening_month)) ~ 0.32,
      n_cluster == 2L ~ 0.15,
      n_cluster == 3L ~ 0.25,
      n_cluster == 4L ~ 0.35,
      n_cluster == 5L ~ 0.45,
      n_cluster > 5L ~ 0.75,
      TRUE ~ 0
    ),
    x_offset = if (n() == 1L) {
      0
    } else {
      seq(-first(cluster_span), first(cluster_span), length.out = n())
    },
    x_cluster = x_plot + x_offset,
    y_plot = map_year_grafik1(opening_year)
  ) %>%
  ungroup()

year_breaks_grafik1 <- c(
  min(data_grafik1_monat_cluster$opening_year, na.rm = TRUE),
  seq(2004, max(data_grafik1_monat_cluster$opening_year, na.rm = TRUE), by = 1)
)

year_breaks_grafik1_plot <- map_year_grafik1(rev(year_breaks_grafik1))
year_breaks_grafik1_labels <- rev(year_breaks_grafik1)

month_breaks_grafik1 <- c(1:11, 13.0, 15.4)


```


```{r grafik1, fig.width=12, fig.height=9.5, out.width="73%", dpi=300, dev="png", fig.align="left", fig.cap='Grafik selbst erstellt auf Basis von Table 1 aus Gu et. al (2021)'}

# Plot von eigener Grafik basierend auf Table 1
plot_data_grafik1_monat <- ggplot(
  data_grafik1_monat_cluster,
  aes(x = x_cluster, y = y_plot, fill = panel, shape = line_status)
) +
  geom_point(
    size = 5.3,
    color = "white",
    stroke = 0.35,
    alpha = 0.95
  ) +
  scale_fill_manual(
    name = "Sample group",
    values = c(
      "Panel A" = "#90343a",
      "Panel B" = "#6d8d84",
      "Panel C" = "#7D8DCA"
    ),
    labels = c(
      "Panel A: new subway lines",
      "Panel B: existing or planned subway lines in treated cities",
      "Panel C: existing or planned subway lines in control cities"
    )
  ) +
  scale_shape_manual(
    name = "\nLine status",
    values = c(
      "Newly opened lines\nAug 2016–Dec 2017" = 21,
      "Existing lines\nopening year ≤ 2017" = 22,
      "Planned lines\nopening year ≥ 2018" = 24
    ),
    labels = c(
      "Newly opened lines",
      "Existing lines",
      "Planned lines"
    )
  ) +
  scale_x_continuous(
    limits = c(0.5, 16.0),
    breaks = month_breaks_grafik1,
    labels = c(
      "Jan", "Feb", "Mär", "Apr", "Mai", "Jun",
      "Jul", "Aug", "Sep", "Okt", "Nov", "Dez",
      "Nur Jahr"
    ),
    expand = c(0.02, 0)
  ) +
  scale_y_continuous(
    breaks = year_breaks_grafik1_plot,
    labels = year_breaks_grafik1_labels,
    minor_breaks = NULL,
    expand = expansion(mult = c(0.03, 0.05))
  ) +
  coord_cartesian(
    clip = "off"
  ) +
  labs(
    title = "Opening dates of subway lines in the sample",
    subtitle = "Zur Übersichtlichkeit wurde der Bereich zwischen 1984 und 2004 ausgeblendet",
    x = NULL,
    y = "Opening year"
  ) +
  guides(
  fill = guide_legend(
    ncol = 1,
    byrow = TRUE,
    order = 1,
    title.position = "top",
    title.hjust = 0,
    label.hjust = 0,
    override.aes = list(shape = 21, size = 5)
  ),
  shape = guide_legend(
    ncol = 1,
    byrow = TRUE,
    order = 2,
    title.position = "top",
    title.hjust = 0,
    label.hjust = 0,
    override.aes = list(fill = "grey70", size = 5)
  )
) +
theme_minimal(base_size = 15) +
theme(
  panel.grid.minor = element_blank(),
  panel.grid.major.x = element_line(color = "grey88", linewidth = 0.35),
  panel.grid.major.y = element_line(color = "grey88", linewidth = 0.35),
  axis.text.y = element_text(size = 12),
  axis.ticks.y = element_line(color = "grey45", linewidth = 0.3),
  axis.title.y = element_text(size = 17, face = "bold", margin = margin(r = 12)),

  plot.title = element_text(
    hjust = 0.5,
    size = 30,
    face = "bold"
  ),
  plot.subtitle = element_text(
    hjust = 0.5,
    size = 20
  ),

  axis.text.x = element_text(size = 18),

  # Legenden nebeneinander
  legend.position = "bottom",
  legend.box = "horizontal",
  legend.direction = "vertical",
  legend.justification = "left",
  legend.box.just = "left",

  # Abstand zwischen Sample group und Line status
  legend.spacing.x = unit(60, "pt"),

  legend.title = element_text(
    size = 18,
    face = "bold",
    hjust = 0
  ),
  legend.text = element_text(
    size = 16,
    hjust = 0,
    lineheight = 1
  ),
  legend.key.size = unit(0.65, "cm"),
  legend.spacing.y = unit(6, "pt"),
  legend.box.spacing = unit(2, "pt"),

  plot.margin = margin(12, 30, 12, 30)
)

plot_data_grafik1_monat
```



# 3 Daten und Sample Construction

Die empirische Analyse von Gu et al. (2021) basiert auf der Verknüpfung von U-Bahn-Eröffnungen, Stadtmerkmalen und hochfrequenten Verkehrsdaten. Zentral ist dabei nicht nur, welche Städte neue U-Bahn-Linien erhalten, sondern auch, welche konkreten Straßenabschnitte durch diese neuen Linien potenziell entlastet werden. Aus den ursprünglichen Rohdaten entsteht dadurch ein Paneldatensatz auf Ebene von Straßenabschnitt und Woche, der für die Difference-in-Differences-Schätzung genutzt wird.

### Datenquellen

Die wichtigste Datenquelle sind stündliche Geschwindigkeitsdaten von Baidu Maps auf Ebene einzelner Straßenabschnitte. Diese Daten entstehen aus Standortinformationen mobiler Geräte, die bei aktivierten Ortungsdiensten regelmäßig an Baidu Maps gesendet werden. Aus diesen Bewegungsdaten berechnet Baidu die durchschnittliche Geschwindigkeit auf einzelnen road segments. Dadurch können die Autoren Verkehrsveränderungen deutlich granularer messen als mit klassischen Haushaltsbefragungen oder aggregierten städtischen Verkehrsdaten.

Ergänzend verwenden die Autoren Daten zu U-Bahn-Linien, ihren Eröffnungsdaten und ihrer geografischen Lage. Diese Informationen sind notwendig, um Treatment-Zeitpunkte zu definieren und Straßenabschnitte räumlich den jeweiligen U-Bahn-Linien zuzuordnen. Zusätzlich werden Stadtmerkmale wie Bevölkerung und BIP pro Kopf verwendet, um systematische Unterschiede zwischen Treatment- und Kontrollstädten zu beschreiben und in der Schätzung für unterschiedliche saisonale Muster zu kontrollieren. Weitere Daten zu öffentlichem Verkehr und Pendelverhalten werden im Paper vor allem für ergänzende Analysen und die Wohlfahrtsinterpretation genutzt, stehen aber nicht im Zentrum der Hauptschätzung.

### Überblick über Datensätze

Die Replikation nutzt einerseits Stadt- und U-Bahn-Daten, mit denen der langfristige Ausbau des chinesischen U-Bahn-Netzes beschrieben wird. Andererseits bilden die Straßenabschnittsdaten die Grundlage der Difference-in-Differences-Analysen: Dort wird verglichen, wie sich Geschwindigkeiten und Stau auf behandelten Straßen relativ zu Kontrollstraßen um den Eröffnungszeitpunkt neuer U-Bahn-Linien entwickeln.

```{r daten-uebersicht, echo=FALSE}
# Überblickstabelle mit Datensatzrollen anlegen
dataset_overview <- tibble::tibble(
  Datensatz = c(
    "Data/UrbanTransitReports.dta",
    "Data/SubwayLines.dta",
    "Data/CityChars.dta",
    "Data/BaseSamp.dta",
    "Data/ExtendSample.dta",
    "Data/WithAgainstTraffic.dta",
    "Data/RushNonrusHours.dta"
  ),
  Datenobjekt = list(
    UrbanTransitReports_input,
    SubwayLines_input,
    CityChars_input,
    BaseSamp_input,
    ExtendSample_input,
    WithAgainstTraffic_input,
    RushNonrusHours_input
  ),
  `Ebene der Beobachtung` = c(
    "Stadt-Jahr",
    "U-Bahn-Linie",
    "Stadt",
    "Straßenabschnitt x Woche",
    "Straßenabschnitt x Woche",
    "Straßenabschnitt x Woche x Rush-Hour-Richtung",
    "Straßenabschnitt x Woche x Zeitfenster"
  ),
  `Für welche Analyse?` = c(
    "Beschreibung des U-Bahn-Ausbaus und der Fahrgastzahlen in Figure 1",
    "Linieneröffnungen und kumulierte Netzlänge in Figure 1",
    "Stadtmerkmale und treated/control-Vergleich in Table 2",
    "Hauptsample für die zentralen DiD-Schätzungen",
    "Erweitertes Sample für Distanz-, Richtungs- und Stau-Heterogenität",
    "Heterogenität nach Fahrtrichtung während der Rush Hour",
    "Vergleich zwischen Rush-Hour- und Non-Rush-Hour-Zeitfenstern"
  ),
  `Aus den Daten berechnete Zusatzinfo` = c(
    paste0(count_distinct(UrbanTransitReports_input, "城市"), " Städte; Jahre ", range_variable(UrbanTransitReports_input, "年份")),
    paste0(count_distinct(SubwayLines_input, "city"), " Städte; Eröffnungsjahre ", range_variable(SubwayLines_input, "openyear")),
    paste0(format_int(sum(CityChars_input$treat == 1, na.rm = TRUE)), " treated cities; ", format_int(sum(CityChars_input$treat == 0, na.rm = TRUE)), " control cities"),
    paste0(count_distinct(BaseSamp_input, "linkid"), " Straßenabschnitte; Wochen relativ zur Eröffnung ", range_variable(BaseSamp_input, "wk2open")),
    paste0(count_distinct(ExtendSample_input, "linkid"), " Straßenabschnitte; Wochen relativ zur Eröffnung ", range_variable(ExtendSample_input, "wk2open")),
    paste0(count_distinct(WithAgainstTraffic_input, "linkid"), " Straßenabschnitte; WithTraffic-Anteil ", share_percent(WithAgainstTraffic_input$WithTraffic == 1), "%"),
    paste0(count_distinct(RushNonrusHours_input, "linkid"), " Straßenabschnitte; Rush-Hour-Anteil ", share_percent(RushNonrusHours_input$rushhour == 1), "%")
  )
) %>%
  # Beobachtungen und Variablen je Datensatz ergänzen
  mutate(
    Beobachtungen = map_chr(Datenobjekt, ~ format_int(nrow(.x))),
    Variablen = map_chr(Datenobjekt, ~ format_int(ncol(.x)))
  ) %>%
  select(Datensatz, `Ebene der Beobachtung`, Beobachtungen, Variablen, `Für welche Analyse?`)

# Überblickstabelle ausgeben
dataset_overview %>%
  kbl(caption = "Übersicht der in der Replikation verwendeten Datensätze", align = c("l", "l", "r", "r", "l", "l")) %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed", "responsive"), full_width = TRUE) %>%
  kable_paper(full_width = TRUE) %>%
  add_footnote("Quelle: Eigene Berechnung aus den eingelesenen .dta-Dateien.")
```



### U-Bahn- und Stadtdaten

Die Treatment-Information stammt aus den Eröffnungen neuer U-Bahn-Linien in chinesischen Städten. Im Untersuchungszeitraum zwischen August 2016 und Dezember 2017 wurden 45 neue Linien beziehungsweise Linienerweiterungen in 25 Städten eröffnet. Für jede dieser Linien wird ein offizielles Eröffnungsdatum verwendet, das den Treatment-Zeitpunkt definiert. Straßenabschnitte in der Nähe einer neuen Linie gelten jedoch nicht automatisch als gleich stark betroffen. Entscheidend ist, ob die neue U-Bahn-Linie für bestimmte Verkehrsbeziehungen tatsächlich eine Alternative zur Autofahrt darstellen kann.

Neben den U-Bahn-Daten spielen die Stadtmerkmale eine wichtige Rolle. Die Kontrollgruppe besteht aus Städten, die zwar bestehende oder geplante U-Bahn-Systeme haben, aber im Untersuchungszeitraum keine neue Linie eröffneten. Diese Städte dienen als Vergleichsgruppe für die Treatment-Städte. Da sich Treatment- und Kontrollstädte jedoch in Größe und wirtschaftlicher Leistungsfähigkeit unterscheiden, werden Stadtmerkmale wie Bevölkerung und BIP pro Kopf nicht nur deskriptiv berichtet, sondern auch methodisch relevant: Sie helfen, unterschiedliche saisonale Verkehrsmuster zwischen Städten zu berücksichtigen.

```{r daten-stadt-u-bahn, echo=FALSE}
# Hilfsfunktion: ganze Zahlen ohne Nachkommastellen, sonst mit 2 Nachkommastellen
format_num_mixed <- function(x, digits = 2) {
  sapply(x, function(z) {
    if (is.na(z)) {
      return(NA_character_)
    }
    
    if (abs(z - round(z)) < 1e-8) {
      format_num(z, 0)
    } else {
      format_num(z, digits)
    }
  })
}

# Letztes verfügbares Jahr bestimmen
latest_urban_year <- max(UrbanTransitReports_input$`年份`, na.rm = TRUE)

# Stadtkennzahlen für das letzte Datenjahr berechnen
urban_context <- UrbanTransitReports_input %>%
  filter(`年份` == latest_urban_year) %>%
  summarise(
    `Städte im letzten Datenjahr` = n_distinct(`城市`),
    `Städte mit U-Bahn` = sum(`是否开通地铁` == 1, na.rm = TRUE),
    `U-Bahn-Länge insgesamt (km)` = sum(`轨道交通运营线路长度公里`, na.rm = TRUE),
    `U-Bahn-Fahrgäste insgesamt (Mio.)` = sum(`轨道交通客运量万人次`, na.rm = TRUE) / 100,
    `Private Pkw insgesamt (10.000 Fahrzeuge)` = sum(`私人汽车拥有量万辆`, na.rm = TRUE),
    `Durchschnittliche Stadtbevölkerung (10.000)` = mean(`年末总人口万`, na.rm = TRUE)
  ) %>%
  pivot_longer(everything(), names_to = "Kennzahl", values_to = "Wert") %>%
  mutate(Wert = format_num_mixed(Wert, 2))

# Kennzahlen zu U-Bahn-Linien berechnen
line_context <- SubwayLines_input %>%
  summarise(
    `U-Bahn-Linien` = n(),
    `Städte` = n_distinct(city),
    `Durchschnittliche Linienlänge (km)` = mean(lengthkm, na.rm = TRUE),
    `Median der Linienlänge (km)` = median(lengthkm, na.rm = TRUE),
    `Durchschnittliche Stationszahl` = mean(stations, na.rm = TRUE),
    `Erste Eröffnung` = min(openyear, na.rm = TRUE),
    `Letzte Eröffnung` = max(openyear, na.rm = TRUE)
  ) %>%
  pivot_longer(everything(), names_to = "Kennzahl", values_to = "Wert") %>%
  mutate(Wert = format_num_mixed(as.numeric(Wert), 2))

# Linienübersicht ausgeben
line_context %>%
  kbl(caption = "SubwayLines.dta: Struktur der U-Bahn-Linien", align = c("l", "r")) %>%
  kable_styling(
    bootstrap_options = c("striped", "hover", "condensed", "responsive"),
    full_width = FALSE
  ) %>%
  kable_paper(full_width = FALSE) %>%
  add_footnote("Quelle: Eigene Berechnung aus SubwayLines.dta.")
```

### Straßenabschnitte und Verkehrsdaten

Für die Verkehrsdaten wählen die Autoren zunächst ausgewählte 5-Kilometer-Abschnitte von neuen, bestehenden und geplanten U-Bahn-Linien aus. Um diese Linienabschnitte wird jeweils ein 2,5-Kilometer-Buffer gelegt. Innerhalb dieser Pufferzonen werden die Straßenabschnitte aus den Baidu-Maps-Daten extrahiert. Dadurch entsteht ein räumlich fokussierter Datensatz von Straßen, die plausibel vom U-Bahn-Ausbau betroffen sein könnten. Das Sample ist deshalb nicht als vollständiges Abbild des gesamten Stadtverkehrs zu verstehen, sondern als gezielte Auswahl relevanter Straßenräume rund um U-Bahn-Linien.

Das Baseline-Sample wird zusätzlich eingeschränkt. Die Autoren konzentrieren sich auf Rush-Hour-Zeiten an Werktagen, weil gerade dort Stau ökonomisch besonders relevant ist und eine Entlastung durch U-Bahnen am wahrscheinlichsten sichtbar wird. Außerdem werden sogenannte directly affected road segments identifiziert. Das sind Straßenabschnitte, die laut Routenplanung von Baidu Maps auf Autofahrten liegen würden, für die die neue U-Bahn-Linie eine plausible Alternative darstellt. Diese Konstruktion ist wichtig, weil sie das Treatment nicht nur räumlich, sondern auch verkehrslogisch definiert: Relevant sind vor allem jene Straßen, auf denen Autoverkehr durch U-Bahn-Nutzung substituiert werden könnte.

```{r daten-strassen-samples, echo=FALSE}
# Verkehrssamples zusammenfassen
traffic_sample_summary <- tibble::tibble(
  Sample = c("BaseSamp", "ExtendSample", "WithAgainstTraffic", "RushNonrusHours"),
  Datenobjekt = list(BaseSamp_input, ExtendSample_input, WithAgainstTraffic_input, RushNonrusHours_input)
) %>%
  mutate(
    Beobachtungen = map_chr(Datenobjekt, ~ format_int(nrow(.x))),
    `Straßenabschnitte` = map_chr(Datenobjekt, ~ count_distinct(.x, "linkid")),
    `Vergleichsfälle` = map_chr(Datenobjekt, ~ count_distinct(.x, "case")),
    `Wochen relativ zur Eröffnung` = map_chr(Datenobjekt, ~ range_variable(.x, "wk2open")),
    `Ø Congestion Index` = map_chr(Datenobjekt, ~ if ("CI" %in% names(.x)) format_num(mean(.x$CI, na.rm = TRUE), 3) else format_num(mean(.x$CI_WithAgainstTraffic, na.rm = TRUE), 3)),
    `Ø residualisierte Log-Geschwindigkeit` = map_chr(Datenobjekt, ~ if ("lnspd_res" %in% names(.x)) format_num(mean(.x$lnspd_res, na.rm = TRUE), 4) else format_num(mean(.x$lnspd_res_WithAgainstTraffic, na.rm = TRUE), 4))
  ) %>%
  select(-Datenobjekt)

# Tabelle der Verkehrssamples ausgeben
traffic_sample_summary %>%
  kbl(caption = "Verkehrssamples: Umfang und zentrale Verkehrsmaße", align = c("l", "r", "r", "r", "l", "r", "r")) %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed", "responsive"), full_width = TRUE) %>%
  kable_paper(full_width = TRUE) %>%
  add_footnote("Quelle: Eigene Berechnung aus den Verkehrsdatensätzen.")

# Treatment- und Kontrollstraßen im Basissample vergleichen
base_treatment_summary <- BaseSamp_input %>%
  mutate(Gruppe = ifelse(treat == 1, "Treated road segments", "Control road segments")) %>%
  group_by(Gruppe) %>%
  summarise(
    Beobachtungen = n(),
    Straßenabschnitte = n_distinct(linkid),
    `Ø Geschwindigkeit (km/h)` = mean(speed, na.rm = TRUE),
    `Ø Congestion Index` = mean(CI, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  mutate(across(where(is.numeric), ~ format_num(.x, 2)))

# Basissample-Vergleich ausgeben
base_treatment_summary %>%
  kbl(caption = "BaseSamp.dta: Treated- und Control-Straßen im Vergleich", align = c("l", "r", "r", "r", "r")) %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed", "responsive"), full_width = FALSE) %>%
  kable_paper(full_width = FALSE) %>%
  add_footnote("Quelle: Eigene Berechnung aus BaseSamp.dta.")

# Heterogenitätsvariablen zusammenfassen
extend_heterogeneity_summary <- ExtendSample_input %>%
  summarise(
    `Direkt betroffene Straßen (%)` = mean(MostAffectedLinks == 1, na.rm = TRUE) * 100,
    `Stärker gestaute Straßen (%)` = mean(MoreCongested == 1, na.rm = TRUE) * 100,
    `Ø Entfernung zur nächsten behandelten Linie (km)` = mean(link2_nearest_treat_line_km, na.rm = TRUE),
    `Ø Entfernung zur nächsten Station (km)` = mean(dist2stn, na.rm = TRUE),
    `Anteil Post-Periode (%)` = mean(post == 1, na.rm = TRUE) * 100
  ) %>%
  pivot_longer(everything(), names_to = "Kennzahl", values_to = "Wert") %>%
  mutate(Wert = format_num(Wert, 2))

# Heterogenitätsübersicht ausgeben
extend_heterogeneity_summary %>%
  kbl(caption = "ExtendSample.dta: Variablen für Heterogenitätsanalysen", align = c("l", "r")) %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed", "responsive"), full_width = FALSE) %>%
  kable_paper(full_width = FALSE) %>%
  add_footnote("Quelle: Eigene Berechnung aus ExtendSample.dta.")
```

### Deskriptive Unterschiede zwischen Treatment und Control

Die deskriptiven Statistiken sind für die Studie methodisch besonders wichtig. Sie zeigen, dass Treatment- und Kontrollstädte keine perfekten Zwillinge sind. Die Treatment-Städte sind im Durchschnitt deutlich größer und etwas wohlhabender als die Kontrollstädte. Das ist für die Identifikation relevant, weil größere und wirtschaftlich stärkere Städte andere Verkehrsdynamiken haben können als kleinere Städte. Besonders problematisch ist dies rund um Chinese New Year, wenn viele Menschen aus großen Städten abreisen und sich die Verkehrsbelastung auch ohne neue U-Bahn-Linie verändern kann.

##### Table 2

```{r table2_helpers, include=FALSE}
# Kleine Hilfsfunktionen für die Replikation von Table 2.
# Die Berechnungslogik folgt ReplicaCodes/Tab2.do: CityChars.dta für Panel A
# und BaseSamp.dta mit wk2open in [-6, 47] für Panel B.
fmt_number <- function(x, digits = 0, big.mark = ",", trim_zeros = FALSE) {
  out <- formatC(
    x,
    format = "f",
    digits = digits,
    big.mark = big.mark,
    preserve.width = "none"
  )

  if (trim_zeros && digits > 0) {
    out <- sub("0+$", "", out)
    out <- sub("\\.$", "", out)
  }

  out
}

fmt_integer <- function(x) {
  formatC(x, format = "d", big.mark = ",")
}

stata_quantile <- function(x, probs) {
  # Inverse empirische Verteilungsfunktion für Quartile
  # Das entspricht der deskriptiven Idee aus Statas summarize
  # und vermeidet Interpolation zwischen zwei Städten
  as.numeric(quantile(x, probs = probs, na.rm = TRUE, type = 1, names = FALSE))
}
```

```{r table2_panel_a, include=FALSE}
# Stadtvariablen für Panel A vorbereiten
table2_city <- CityChars_input %>%
  mutate(pop_million = popx10k / 100)

# Kennzahlen für eine Stadtvariable berechnen
summarise_city_var <- function(data, variable, digits) {
  values <- data[[variable]]
  c(
    mean = round(mean(values, na.rm = TRUE), digits),
    p25 = round(stata_quantile(values, 0.25), digits),
    median = round(stata_quantile(values, 0.50), digits),
    p75 = round(stata_quantile(values, 0.75), digits)
  )
}

# Eine Tabellenzeile für eine Stadtgruppe erzeugen
make_city_stats_row <- function(variable_label, group_label, treat_value, source_variable, digits) {
  group_data <- table2_city %>%
    filter(treat == treat_value)
  stats <- summarise_city_var(group_data, source_variable, digits)

  tibble(
    variable = variable_label,
    group = group_label,
    n_cities = nrow(group_data),
    digits = digits,
    mean = stats[["mean"]],
    p25 = stats[["p25"]],
    median = stats[["median"]],
    p75 = stats[["p75"]]
  )
}

# Kennzahlen für behandelte und Kontrollstädte sammeln
city_stats <- bind_rows(
  make_city_stats_row("population (million)", "treated cities", 1, "pop_million", 2),
  make_city_stats_row("GDP per capita (yuan)", "treated cities", 1, "gdppc", 0),
  make_city_stats_row("population (million)", "control cities", 0, "pop_million", 2),
  make_city_stats_row("GDP per capita (yuan)", "control cities", 0, "gdppc", 0)
)

# Panel A in das Tabellenformat bringen
table2_panel_a <- city_stats %>%
  select(variable, group, n_cities, digits, mean, p25, median, p75) %>%
  pivot_wider(
    names_from = group,
    values_from = c(n_cities, digits, mean, p25, median, p75),
    names_glue = "{group}_{.value}"
  ) %>%
  mutate(
    across(
      c(`treated cities_mean`, `treated cities_p25`, `treated cities_median`, `treated cities_p75`),
      ~ if_else(
        `treated cities_digits` == 0,
        fmt_number(.x, digits = 0),
        fmt_number(.x, digits = 2, trim_zeros = TRUE)
      )
    ),
    across(
      c(`control cities_mean`, `control cities_p25`, `control cities_median`, `control cities_p75`),
      ~ if_else(
        `control cities_digits` == 0,
        fmt_number(.x, digits = 0),
        fmt_number(.x, digits = 2, trim_zeros = TRUE)
      )
    )
  ) %>%
  transmute(
    variable,
    treated_mean = `treated cities_mean`,
    treated_p25 = `treated cities_p25`,
    treated_median = `treated cities_median`,
    treated_p75 = `treated cities_p75`,
    control_mean = `control cities_mean`,
    control_p25 = `control cities_p25`,
    control_median = `control cities_median`,
    control_p75 = `control cities_p75`
  )

table2_n_treated <- 25L # Table 2 zählt Foshan als eigene treated city; CityChars fasst Foshan mit Guangzhou zusammen.
table2_n_control <- unique(city_stats$n_cities[city_stats$group == "control cities"])
```

```{r table2_panel_b, include=FALSE}
# Straßendaten auf das Table-2-Fenster begrenzen
table2_roads <- BaseSamp_input %>%
  filter(between(wk2open, -6, 47)) %>%
  mutate(
    treatment_group = if_else(treat == 1, "treated", "control"),
    road_type = factor(
      roadtype,
      levels = c(1, 2, 3, 4),
      labels = c("highways", "urban expressways", "arterial streets", "sub-arterial streets")
    )
  )

# Kennzahlen für Straßen nach Gruppe und Typ berechnen
summarise_roads <- function(data, group_value, road_value = NULL) {
  sample_data <- data %>%
    filter(treatment_group == group_value)

  if (!is.null(road_value)) {
    sample_data <- sample_data %>%
      filter(road_type == road_value)
  }

  tibble(
    n_obs = nrow(sample_data),
    n_segments = n_distinct(sample_data$linkid),
    avg_speed = mean(sample_data$speed, na.rm = TRUE),
    avg_congestion_index = mean(sample_data$CI, na.rm = TRUE)
  )
}

# Zeilenreihenfolge für Panel B festlegen
road_rows <- c("all road segments", levels(table2_roads$road_type))

# Rohwerte für Panel B berechnen
table2_panel_b_raw <- bind_rows(
  lapply(road_rows, function(row_name) {
    road_filter <- if (row_name == "all road segments") NULL else row_name

    bind_cols(
      tibble(road_type = row_name),
      summarise_roads(table2_roads, "treated", road_filter) %>%
        rename_with(~ paste0("treated_", .x)),
      summarise_roads(table2_roads, "control", road_filter) %>%
        rename_with(~ paste0("control_", .x))
    )
  })
)

# Panel B für die Ausgabe formatieren
table2_panel_b <- table2_panel_b_raw %>%
  transmute(
    road_type,
    treated_n_obs = fmt_integer(treated_n_obs),
    treated_n_segments = fmt_integer(treated_n_segments),
    treated_avg_speed = fmt_number(round(treated_avg_speed, 2), digits = 2, trim_zeros = TRUE),
    treated_avg_congestion_index = fmt_number(round(treated_avg_congestion_index, 2), digits = 2, trim_zeros = TRUE),
    control_n_obs = fmt_integer(control_n_obs),
    control_n_segments = fmt_integer(control_n_segments),
    control_avg_speed = fmt_number(round(control_avg_speed, 2), digits = 2, trim_zeros = TRUE),
    control_avg_congestion_index = fmt_number(round(control_avg_congestion_index, 2), digits = 2, trim_zeros = TRUE)
  )
```

```{r table2_original_kableextra, results='asis', echo=FALSE, message=FALSE, warning=FALSE}
# Zusatzpaket für die HTML-Tabelle laden
library(kableExtra)

# Labels etwas näher am Original
panel_a_out <- table2_panel_a %>%
  mutate(
    variable = recode(
      variable,
      "population (million)" = "Population (million)",
      "GDP per capita (yuan)" = "GDP per capita (yuan)"
    )
  ) %>%
  transmute(
    variable,
    t1 = treated_mean,
    t2 = treated_p25,
    t3 = treated_median,
    t4 = treated_p75,
    gap = "",
    c1 = control_mean,
    c2 = control_p25,
    c3 = control_median,
    c4 = control_p75
  )

panel_b_out <- table2_panel_b %>%
  mutate(
    road_type = recode(
      road_type,
      "all road segments" = "All road segments",
      "highways" = "Highways",
      "urban expressways" = "Urban expressways",
      "arterial streets" = "Arterial streets",
      "sub-arterial streets" = "Subarterial streets"
    )
  ) %>%
  transmute(
    variable = road_type,
    t1 = treated_n_obs,
    t2 = treated_n_segments,
    t3 = treated_avg_speed,
    t4 = treated_avg_congestion_index,
    gap = "",
    c1 = control_n_obs,
    c2 = control_n_segments,
    c3 = control_avg_speed,
    c4 = control_avg_congestion_index
  )

# Eine gemeinsame Tabelle bauen
table2_out <- bind_rows(
  tibble(
  variable = '<span class="panel-label">Panel A. City-level characteristics</span>',
  t1 = "", t2 = "", t3 = "", t4 = "",
  gap = "",
  c1 = "", c2 = "", c3 = "", c4 = ""
  ),
  panel_a_out,
  tibble(
  variable = "",
  t1 = "Observations",
  t2 = "Number<br>of unique<br>segments",
  t3 = "Average<br>speed<br>(km/h)",
  t4 = "Average<br>congest.<br>index",
  gap = "",
  c1 = "Observations",
  c2 = "Number<br>of unique<br>segments",
  c3 = "Average<br>speed<br>(km/h)",
  c4 = "Average<br>congest.<br>index"
  ),
  tibble(
  variable = '<span class="panel-label">Panel B. Road speed and congestion</span>',
  t1 = "", t2 = "", t3 = "", t4 = "",
  gap = "",
  c1 = "", c2 = "", c3 = "", c4 = ""
  ),
  panel_b_out
)

kable(
  table2_out,
  format = "html",
  escape = FALSE,
  align = c("l", rep("c", 4), "c", rep("c", 4)),
  col.names = c(
    "",
    "Mean", "p25", "Median", "p75",
    "",
    "Mean", "p25", "Median", "p75"
  ),
  table.attr = 'class="table2-original replicated-original-table"'
) %>%
  add_header_above(
    setNames(
      c(1, 4, 1, 4),
      c(
        " ",
        paste0("Treated cities (<em>N</em> = ", table2_n_treated, ")"),
        " ",
        paste0("Control cities (<em>N</em> = ", table2_n_control, ")")
      )
    ),
    escape = FALSE,
    line = FALSE
  ) %>%
  kable_styling(
    full_width = FALSE,
    position = "center",
    bootstrap_options = NULL,
    font_size = 18,
    html_font = "Times New Roman"
  ) %>%
  row_spec(0, bold = FALSE) %>%
  row_spec(1, italic = TRUE, extra_css = "text-align: left;") %>%
  row_spec(4, extra_css = "border-top: 24px solid white; border-bottom: 1.5px solid #222;") %>%
  row_spec(5, italic = TRUE, extra_css = "text-align: left;") %>%
  column_spec(1, width = "250px") %>%
  column_spec(2:5, width = "95px") %>%
  column_spec(6, width = "30px") %>%
  column_spec(7:10, width = "95px") %>%
  footnote(
    general = paste0(
      "Data in panel A are from the 2017 Statistical Yearbook of Chinese Cities. ",
      "In panel B, each observation is a road segment-by-week-to-opening. ",
      "Segments in the baseline regression sample are included. ",
      "Treated road segments are those directly affected by the new subway lines. ",
      "Week-to-opening is between 6 weeks before and 48 weeks after line opening."
    ),
    general_title = "Notes:",
    escape = FALSE,
    footnote_as_chunk = TRUE
  )
```


```{r table2_figma, echo=FALSE, include=FALSE, message=FALSE, warning=FALSE, results='asis'}
# Vereinfachte Table-2-Version für Figma:
# Nur Mittelwerte, getrennt nach treated und control cities.


table2_figma <- bind_rows(
  table2_panel_a %>%
    filter(variable == "population (million)") %>%
    transmute(
      variable = "Bevölkerung (Mio.)",
      `Treated cities` = treated_mean,
      `Control cities` = control_mean
    ),
  
  table2_panel_a %>%
    filter(variable == "GDP per capita (yuan)") %>%
    transmute(
      variable = "BIP pro Kopf (yuan)",
      `Treated cities` = treated_mean,
      `Control cities` = control_mean
    ),
  
  table2_panel_b %>%
    filter(road_type == "all road segments") %>%
    transmute(
      variable = "Durchschnittsgeschwindigkeit (km/h)",
      `Treated cities` = treated_avg_speed,
      `Control cities` = control_avg_speed
    ),
  
  table2_panel_b %>%
    filter(road_type == "all road segments") %>%
    transmute(
      variable = "Congestion index",
      `Treated cities` = treated_avg_congestion_index,
      `Control cities` = control_avg_congestion_index
    )
)

kable(
  table2_figma,
  format = "html",
  escape = FALSE,
  align = c("l", "c", "c"),
  col.names = c("Mittelwert von", "Treated cities", "Control cities"),
  table.attr = 'class="table2-figma"'
) %>%
  kable_styling(
    full_width = FALSE,
    position = "center",
    bootstrap_options = NULL,
    font_size = 18,
    html_font = "Times New Roman"
  ) %>%
  column_spec(1, width = "260px") %>%
  column_spec(2:3, width = "160px") %>%
  row_spec(0, bold = TRUE) %>%
  footnote(
    general = paste0(
      ""
    ),
    general_title = "",
    footnote_as_chunk = TRUE,
    escape = FALSE
  )
```
*Grafik selbst erstellt in Anlehnung an Table 2 aus Gu et. al (2021).*

```{css, echo=FALSE}
/*
Die Tabellen ab Table 2 replizieren bewusst das Original-Layout des Papers.
prettydoc/html_pretty formatiert alle Tabellen innerhalb von `.main-content`
pauschal grün/grau. Diese Regeln werden hier nur für die replizierten Tabellen
überschrieben, damit frühere Überblickstabellen weiterhin das prettydoc-Design
behalten.
*/
.main-content table.replicated-original-table,
.main-content table.replicated-original-table.table {
  width: auto !important;
  color: #000 !important;
  background: transparent !important;
  border-left: none !important;
  border-right: none !important;
  box-shadow: none !important;
  --bs-table-bg: transparent !important;
  --bs-table-color: #000 !important;
  --bs-table-accent-bg: transparent !important;
  --bs-table-striped-bg: transparent !important;
  --bs-table-striped-color: #000 !important;
  --bs-table-hover-bg: transparent !important;
  --bs-table-hover-color: #000 !important;
}

.main-content table.replicated-original-table tr,
.main-content table.replicated-original-table tr:nth-child(odd),
.main-content table.replicated-original-table tr:nth-child(even),
.main-content table.replicated-original-table thead tr,
.main-content table.replicated-original-table tbody tr,
.main-content table.replicated-original-table tfoot tr,
.main-content table.replicated-original-table.table-hover > tbody > tr:hover,
.main-content table.replicated-original-table.table-striped > tbody > tr:nth-of-type(odd) {
  color: #000 !important;
  background: transparent !important;
  background-color: transparent !important;
  background-image: none !important;
  box-shadow: none !important;
}

.main-content table.replicated-original-table.table > :not(caption) > * > *,
.main-content table.replicated-original-table caption,
.main-content table.replicated-original-table th,
.main-content table.replicated-original-table td {
  color: #000 !important;
  background: transparent !important;
  background-color: transparent !important;
  background-image: none !important;
  box-shadow: none !important;
}

/* Grundlayout */
.table2-original {
  width: 1000px !important;
  max-width: 100% !important;
  table-layout: fixed !important;
  border-collapse: collapse !important;
  border-top: 2px solid #222 !important;
  border-bottom: 2px solid #222 !important;
  margin-top: 10px !important;
  margin-left: auto !important;
  margin-right: auto !important;
  font-family: "Times New Roman", Times, serif !important;
}

/* Caption ausblenden */
.table2-original caption {
  display: none !important;
}

/* Alle Bootstrap-/kableExtra-Standardlinien entfernen */
.table2-original.table > :not(caption) > * > *,
.table2-original th,
.table2-original td {
  border-top: none !important;
  border-bottom: none !important;
  border-left: none !important;
  border-right: none !important;
  box-shadow: none !important;
  background-color: transparent !important;
}

/* Zellen allgemein */
.table2-original th,
.table2-original td {
  padding: 3px 8px !important;
  line-height: 1.08 !important;
  overflow: visible !important;
  vertical-align: middle !important;
  font-weight: normal !important;
}

/* Linke Spalte schmal halten */
.table2-original th:first-child,
.table2-original td:first-child {
  width: 180px !important;
  min-width: 180px !important;
  max-width: 180px !important;
  text-align: left !important;
  position: relative !important;
}

/* Alle anderen Spalten zentrieren */
.table2-original th:not(:first-child),
.table2-original td:not(:first-child) {
  text-align: center !important;
}

/* Gap-Spalte zwischen treated und control */
.table2-original th:nth-child(6),
.table2-original td:nth-child(6) {
  width: 34px !important;
  min-width: 34px !important;
  max-width: 34px !important;
  padding-left: 10px !important;
  padding-right: 10px !important;
  border-top: none !important;
  border-bottom: none !important;
}

/* Header: keine automatischen Linien */
.table2-original thead th {
  border-top: none !important;
  border-bottom: none !important;
  font-weight: normal !important;
}

/* Kurze Linien unter Treated cities / Control cities */
.table2-original thead tr:first-child th:nth-child(2),
.table2-original thead tr:first-child th:nth-child(4) {
  border-bottom: 1.5px solid #222 !important;
}

/* Linie unter Mean / p25 / Median / p75 */
.table2-original thead tr:nth-child(2) th:not(:first-child):not(:nth-child(6)) {
  border-bottom: 1.5px solid #222 !important;
}

/* Keine Linie in der linken Header-Zelle und in der Gap-Spalte */
.table2-original thead tr:first-child th:nth-child(1),
.table2-original thead tr:first-child th:nth-child(3),
.table2-original thead tr:nth-child(2) th:nth-child(1),
.table2-original thead tr:nth-child(2) th:nth-child(6) {
  border-bottom: none !important;
}

/* Panel-A-/Panel-B-Labels: ragen optisch in die Tabelle hinein,
   vergrößern aber NICHT die erste Spalte */
.table2-original .panel-label {
  position: absolute !important;
  left: 0 !important;
  bottom: 2px !important;
  white-space: nowrap !important;
  font-style: italic !important;
  font-weight: normal !important;
  width: max-content !important;
  z-index: 5 !important;
  pointer-events: none !important;
}

/* Panel-Zeilen */
.table2-original tbody tr:nth-child(1) td,
.table2-original tbody tr:nth-child(5) td {
  height: 24px !important;
  padding-top: 0 !important;
  padding-bottom: 2px !important;
  vertical-align: bottom !important;
  font-style: italic !important;
}

/* Besonders Panel B untenbündig */
.table2-original tbody tr:nth-child(5) td {
  vertical-align: bottom !important;
  padding-top: 0 !important;
  padding-bottom: 2px !important;
}

/* Road-segment-Überschriften nicht fett */
.table2-original tbody tr:nth-child(4) td {
  font-weight: normal !important;
  vertical-align: bottom !important;
}

/* Linie oberhalb und unterhalb der Panel-B-Spaltenüberschriften */
.table2-original tbody tr:nth-child(4) td:not(:nth-child(6)) {
  border-top: 1.5px solid #222 !important;
  border-bottom: 1.5px solid #222 !important;
  padding-top: 12px !important;
  padding-bottom: 7px !important;
}

/* Keine Linie in der Gap-Spalte bei Panel-B-Überschriften */
.table2-original tbody tr:nth-child(4) td:nth-child(6) {
  border-top: none !important;
  border-bottom: none !important;
}

/* Notes */
.table2-original tfoot td {
  border-top: 2px solid #222 !important;
  font-size: 17px !important;
  line-height: 1.18 !important;
  text-align: left !important;
  padding-top: 8px !important;
  font-weight: normal !important;
}

.table2-original tfoot {
  font-weight: normal !important;
}

/* Auch fett gesetzte Inhalte überschreiben */
.table2-original strong,
.table2-original b {
  font-weight: normal !important;
}
```


Table 2 ist daher nicht nur eine reine Datenbeschreibung, sondern eine erste Warnung für die Identifikation. Wenn Treatment-Städte systematisch größer und reicher sind, reicht ein einfacher Vorher-Nachher-Vergleich nicht aus. Ein beobachteter Anstieg der Straßengeschwindigkeit nach einer U-Bahn-Eröffnung könnte sonst teilweise auf unterschiedliche saisonale Muster oder allgemeine Stadtunterschiede zurückzuführen sein. Genau deshalb verwenden Gu et al. (2021) ein Difference-in-Differences-Design mit Fixed Effects und zusätzlichen Kontrollen für unterschiedliche Saisonalität nach Stadtmerkmalen.





# 4 Empirische Strategie

Die empirische Strategie von Gu et al. (2021) zielt darauf ab, den kausalen Effekt neuer U-Bahn-Linien auf die Straßengeschwindigkeit zu schätzen. Ein einfacher Vergleich der Geschwindigkeit vor und nach einer Eröffnung wäre problematisch, weil sich Verkehr über die Zeit auch aus anderen Gründen verändert, etwa durch Wochentage, Rush-Hour-Muster, saisonale Effekte oder Chinese New Year. Deshalb kombinieren die Autoren hochfrequente Straßendaten mit einem Difference-in-Differences-Design, das behandelte Straßenabschnitte mit Kontrollstraßen vergleicht und gleichzeitig viele zeitliche und räumliche Störfaktoren herausrechnet.

### Outcome: residualisierte Straßengeschwindigkeit

Die abhängige Variable der empirischen Analyse ist nicht die rohe Straßengeschwindigkeit, sondern eine residualisierte logarithmierte Geschwindigkeit. Der Grund dafür ist, dass sich Geschwindigkeit systematisch nach Straßenabschnitt, Wochentag und Tageszeit unterscheidet. Ein zentraler Straßenabschnitt während der Rush Hour ist beispielsweise grundsätzlich langsamer als derselbe Abschnitt zu einer weniger stark belasteten Tageszeit. Um solche stabilen Muster herauszurechnen, schätzen Gu et al. (2021) zunächst folgende Hilfsregression:

$$
\ln(speed_{lt}) = \lambda_{l,dow,h} + \varepsilon_{lt}
$$

Dabei bezeichnet $speed_{lt}$ die Geschwindigkeit auf Straßenabschnitt $l$ zum Zeitpunkt $t$. Die Fixed Effects $\lambda_{l,dow,h}$ kontrollieren für die Kombination aus Straßenabschnitt, Wochentag und Stunde. Der Residualterm $\varepsilon_{lt}$ misst damit, ob ein Straßenabschnitt zu einem bestimmten Zeitpunkt schneller oder langsamer ist als für genau diesen Straßenabschnitt, diesen Wochentag und diese Stunde üblich. Diese Residuen werden anschließend auf Wochenebene aggregiert. Das Outcome der Hauptschätzung ist somit die wöchentliche Abweichung der logarithmierten Geschwindigkeit vom normalen Verkehrsmuster eines Straßenabschnitts.

Diese Residualisierung ist methodisch wichtig, weil sie typische Tages- und Wochenmuster bereits vor der eigentlichen Difference-in-Differences-Schätzung entfernt. Die Analyse basiert dadurch nicht darauf, dass manche Straßen grundsätzlich schneller oder langsamer sind, sondern auf Veränderungen relativ zum jeweils normalen Geschwindigkeitsniveau. Ein positiver Treatment-Effekt bedeutet daher, dass Straßenabschnitte nach der Eröffnung einer U-Bahn-Linie schneller werden, als es aufgrund ihres üblichen Verkehrsmusters zu erwarten wäre.

### Stacked Difference-in-Differences

Zur Schätzung des kausalen Effekts verwenden Gu et al. (2021) ein stacked Difference-in-Differences-Design. Jede neue U-Bahn-Linie bildet dabei eine eigene Vergleichsgruppe. Die direkt betroffenen Straßenabschnitte in der jeweiligen Treatment-Stadt werden mit Straßenabschnitten aus Kontrollstädten verglichen, die im Untersuchungszeitraum keine neue U-Bahn-Linie eröffneten. Den Kontrollstraßen wird künstlich dasselbe Eröffnungsdatum zugewiesen wie der jeweiligen Treatment-Linie. Dadurch wird für jede Eröffnung ein Vergleich zwischen behandelten und unbehandelten Straßenabschnitten im selben Kalenderzeitraum konstruiert.

Die zentrale dynamische Schätzgleichung lautet:

$$
\widetilde{\ln(speed)}_{lgw}
=
\sum_{\substack{w=\underline{w} \ w \neq -1}}^{\overline{w}}
\beta_w \cdot T_{lg} \cdot d_{gw}
+
\lambda_l
+
\lambda_{gw}
+
\gamma_t \cdot d_t \cdot X_c
+
\varepsilon_{lgw}
$$

Die abhängige Variable $\widetilde{\ln(speed)}*{lgw}$ ist die wöchentliche residualisierte logarithmierte Geschwindigkeit eines Straßenabschnitts. $T*{lg}$ zeigt an, ob ein Straßenabschnitt zur Treatment-Gruppe gehört. $d_{gw}$ ist ein Indikator für die Woche relativ zur Eröffnung der jeweiligen U-Bahn-Linie. Die Koeffizienten $\beta_w$ messen damit den Effekt der U-Bahn-Eröffnung in jeder Woche vor und nach dem Treatment. Die Woche direkt vor der Eröffnung, also $w = -1$, wird ausgelassen und dient als Referenzperiode.

Die Fixed Effects $\lambda_l$ kontrollieren für zeitinvariante Unterschiede zwischen Straßenabschnitten, etwa Lage, Straßentyp oder dauerhaft unterschiedliche Verkehrsbelastung. Die group-by-week-to-opening Fixed Effects $\lambda_{gw}$ kontrollieren für zeitliche Muster, die innerhalb einer Vergleichsgruppe gemeinsam auftreten. Der zusätzliche Term $\gamma_t \cdot d_t \cdot X_c$ erlaubt es, dass saisonale Effekte je nach Stadtmerkmalen wie Bevölkerung und BIP pro Kopf unterschiedlich ausfallen. Das ist besonders wichtig, weil viele U-Bahn-Eröffnungen am Jahresende stattfanden und damit nahe an Chinese New Year liegen.


### Identifikationsannahme und mögliche Probleme

Die zentrale Identifikationsannahme ist eine Parallel-Trends-Annahme: Ohne neue U-Bahn-Linie hätten sich die behandelten Straßenabschnitte ähnlich entwickelt wie die Kontrollstraßen. Anders formuliert: Der beobachtete Unterschied nach der Eröffnung darf nicht durch andere zeitgleich eintretende Faktoren verursacht werden, die nur die Treatment-Städte oder die betroffenen Straßenabschnitte betreffen. Die Autoren prüfen diese Annahme unter anderem über die Pre-Treatment-Koeffizienten in der Event-Study-Grafik. Wenn diese vor der Eröffnung nahe bei null liegen, spricht das dafür, dass es keine offensichtlichen unterschiedlichen Trends vor dem Treatment gab.

Trotzdem bleibt die Identifikation anspruchsvoll. Ein erstes Problem ist, dass U-Bahn-Linien nicht zufällig gebaut werden. Sie entstehen typischerweise dort, wo Verkehrsprobleme besonders groß sind oder wo starkes Stadtwachstum erwartet wird. Für die interne Validität ist das nicht automatisch fatal, solange sich die Trends vor der Eröffnung nicht systematisch unterscheiden. Für die Interpretation bedeutet es aber, dass der geschätzte Effekt eher für verkehrlich relevante, gezielt ausgewählte U-Bahn-Korridore gilt und nicht für zufällig platzierte Linien.

Ein zweites und besonders wichtiges Problem ist das Treatment Timing. Viele U-Bahn-Linien wurden am Jahresende eröffnet, also kurz vor Chinese New Year. In dieser Zeit verändern sich Verkehrsmuster in chinesischen Städten ohnehin stark, weil viele Menschen verreisen und wirtschaftliche Aktivität zurückgeht. Dieses Problem wird dadurch verschärft, dass Treatment-Städte im Durchschnitt größer und wohlhabender sind als Kontrollstädte und deshalb potenziell stärker von saisonalen Verkehrsrückgängen betroffen sind. Die Autoren adressieren dies, indem sie unterschiedliche Saisonalität nach Stadtmerkmalen kontrollieren und zusätzliche Robustheitschecks mit unterschiedlichen Zeitfenstern und Teilstichproben durchführen.

Ein weiteres mögliches Problem betrifft Bauarbeiten und Antizipationseffekte. U-Bahn-Bau kann Straßenverkehr bereits vor der offiziellen Eröffnung beeinflussen, etwa durch Baustellen oder temporäre Straßensperrungen. Deshalb beschränken die Autoren das Baseline-Fenster vor der Eröffnung auf die letzten sechs Wochen, in denen größere Bauarbeiten typischerweise abgeschlossen sein sollten. Gleichzeitig könnten Haushalte oder Pendler ihr Verhalten bereits vor der Eröffnung anpassen, weil die neue Linie lange im Voraus bekannt ist. Diese Punkte sind keine vollständige Widerlegung der Ergebnisse, aber sie zeigen, dass der geschätzte Effekt nur dann kausal interpretiert werden kann, wenn die verbleibenden zeitgleichen Veränderungen ausreichend durch Fixed Effects, Saisonalitätskontrollen und Robustheitschecks aufgefangen werden.


# 5 Hauptergebnisse

Die Hauptergebnisse zeigen, dass neue U-Bahn-Linien die Straßengeschwindigkeit auf direkt betroffenen Straßenabschnitten erhöhen. Entscheidend ist dabei nicht nur, ob nach der Eröffnung ein positiver Effekt sichtbar wird, sondern auch, ob sich bereits vor der Eröffnung unterschiedliche Trends zwischen Treatment- und Kontrollstraßen zeigen. Deshalb werden zunächst die dynamischen Effekte rund um den Eröffnungszeitpunkt betrachtet und anschließend die durchschnittlichen Baseline-Effekte in einer Regressions-Tabelle zusammengefasst.

### Dynamische Effekte um die U-Bahn-Eröffnung

##### Figure 2
```{r fig2_data, echo=FALSE}
# Basissample auf das Event-Study-Fenster begrenzen
base_samp <- BaseSamp_input %>%
  filter(between(wk2open, -6, 47))

# Stata Fig2.do baut exakt diese Event-Variablen:
# Am6-Am2, A0, Ap1-Ap47 (Am1 ist ausgelassene Referenzperiode)
for (w in -6:-2) {
  nm <- paste0("Am", abs(w))
  base_samp[[nm]] <- base_samp$treat * as.numeric(base_samp$wk2open == w)
}
base_samp$A0 <- base_samp$treat * as.numeric(base_samp$wk2open == 0)
for (w in 1:47) {
  nm <- paste0("Ap", w)
  base_samp[[nm]] <- base_samp$treat * as.numeric(base_samp$wk2open == w)
}

rhs_terms <- c(paste0("Am", 6:2), "A0", paste0("Ap", 1:47))
rhs_str <- paste(rhs_terms, collapse = " + ")

# Panel A: stacked DID (wie Fig2.do)
form_a <- as.formula(
  paste0("lnspd_res ~ ", rhs_str, " + treat | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc]")
)
mod_fig2_a <- feols(form_a, cluster = ~case, data = base_samp)

# Panel B: TWFE, treated units only
form_b <- as.formula(
  paste0("lnspd_res ~ ", rhs_str, " | linkid + yrwk")
)
mod_fig2_b <- feols(form_b, cluster = ~case, data = base_samp %>% filter(treat == 1))

# Panel C: TWFE + differential seasonality, treated units only
form_c <- as.formula(
  paste0("lnspd_res ~ ", rhs_str, " | linkid + yrwk + yrwk[lnpop] + yrwk[lngdppc]")
)
mod_fig2_c <- feols(form_c, cluster = ~case, data = base_samp %>% filter(treat == 1))

extract_event_path <- function(model) {
  td <- broom::tidy(model)

  pre <- map_dfr(-6:-2, function(w) {
    key <- paste0("Am", abs(w))
    hit <- td %>% filter(term == key)
    tibble(
      week = w,
      estimate = hit$estimate[1],
      std_error = hit$std.error[1]
    )
  })

  ref <- tibble(week = -1, estimate = 0, std_error = 0)

  zero <- td %>% filter(term == "A0") %>%
    transmute(week = 0, estimate = estimate, std_error = std.error)

  post <- map_dfr(1:47, function(w) {
    key <- paste0("Ap", w)
    hit <- td %>% filter(term == key)
    tibble(
      week = w,
      estimate = hit$estimate[1],
      std_error = hit$std.error[1]
    )
  })

  bind_rows(pre, ref, zero, post) %>%
    mutate(
      ci_low = estimate - 1.96 * std_error,
      ci_high = estimate + 1.96 * std_error
    )
}

fig2_a_df <- extract_event_path(mod_fig2_a)
fig2_b_df <- extract_event_path(mod_fig2_b)
fig2_c_df <- extract_event_path(mod_fig2_c)

plot_fig2_panel <- function(df, panel_title) {
  ggplot(df, aes(x = week, y = estimate)) +
    geom_errorbar(aes(ymin = ci_low, ymax = ci_high), width = 0.5, color = "#6d8d84", linewidth = 0.5) +
    geom_point(color = "#6d8d84", size = 1.7) +
    geom_vline(xintercept = 0, color = "#a63d40", linewidth = 0.6) +
    scale_x_continuous(breaks = seq(-8, 48, 4), limits = c(-8, 48)) +
    scale_y_continuous(breaks = seq(-0.05, 0.1, 0.05), limits = c(-0.055, 0.105)) +
    labs(title = panel_title, x = "Weeks to subway opening", y = "Coefficient") +
    theme_minimal(base_size = 11) +
    theme(
      plot.title = element_text(hjust = 0, face = "plain"),
      panel.grid.minor = element_blank()
    )
}

p_fig2_a <- plot_fig2_panel(fig2_a_df, "Panel A. Stacked DID")
p_fig2_b <- plot_fig2_panel(fig2_b_df, "Panel B. Two-way fixed effects, not accounting for seasonality")
p_fig2_c <- plot_fig2_panel(fig2_c_df, "Panel C. Two-way fixed effects, accounting for seasonality")
```


```{r fig2_panel_a, include=FALSE, fig.width=7, fig.height=4.5, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 2 aus Gu et. al (2021)'}
# Panel A separat ausgeben
p_fig2_a
```


```{r fig2_panel_b, include=FALSE, fig.width=7, fig.height=4.5, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 2 aus Gu et. al (2021)'}
# Panel B separat ausgeben
p_fig2_b
```


```{r fig2_panel_c, include=FALSE, fig.width=7, fig.height=4.5, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 2 aus Gu et. al (2021)'}
# Panel C separat ausgeben
p_fig2_c
```


```{r fig2_combined, echo=FALSE, fig.width=10, fig.height=7.5, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 2 aus Gu et. al (2021)'}
# Drei Panels zu einer Abbildung kombinieren
(
  (p_fig2_a | p_fig2_b) /
  (p_fig2_c | plot_spacer())
) +
  plot_layout(
    widths = c(1, 1),
    heights = c(1, 1)
  )
```





Figure 2 zeigt die geschätzten dynamischen Treatment-Effekte in den Wochen vor und nach der Eröffnung einer neuen U-Bahn-Linie. Auf der x-Achse sind die Wochen relativ zur Eröffnung abgetragen, wobei Woche 0 den Zeitpunkt der Inbetriebnahme markiert. Auf der y-Achse stehen die geschätzten Koeffizienten der Event-Study-Spezifikation. Da die abhängige Variable die residualisierte logarithmierte Geschwindigkeit ist, können die Koeffizienten näherungsweise als prozentuale Veränderung der Straßengeschwindigkeit interpretiert werden.

Panel A zeigt die Hauptspezifikation mit dem stacked Difference-in-Differences-Design. Vor der Eröffnung liegen die Koeffizienten nahe bei null, was gegen starke unterschiedliche Pre-Trends zwischen Treatment- und Kontrollstraßen spricht. Direkt nach der Eröffnung steigen die Koeffizienten deutlich an. Der Effekt liegt in den ersten Wochen bereits bei ungefähr 2 bis 3 Prozent, erreicht nach einigen Wochen Werte um etwa 5 Prozent und stabilisiert sich anschließend überwiegend im Bereich von etwa 3 bis 4 Prozent. Das spricht dafür, dass neue U-Bahn-Linien relativ schnell zu einer messbaren Entlastung nahegelegener Straßen führen.

Panels B und C dienen als Vergleichsspezifikationen mit Two-way Fixed Effects. Panel B berücksichtigt keine zusätzliche Anpassung für unterschiedliche Saisonalität, während Panel C für solche saisonalen Unterschiede kontrolliert. Beide Panels zeigen ein ähnliches Muster: Nach der U-Bahn-Eröffnung steigen die geschätzten Effekte an und bleiben über viele Wochen positiv. Die Schätzungen sind teilweise weniger präzise als im stacked-DID-Modell, aber die grundlegende Aussage bleibt erhalten. Damit stützt Figure 2 die Interpretation, dass der beobachtete Geschwindigkeitsanstieg nicht nur ein Artefakt einer einzelnen Spezifikation ist.



### Baseline Regression

##### Table 3

```{r table3_baseline_estimates, results='asis', echo=FALSE}
# Treatment-Post-Interaktion für Table 3 erzeugen
tab3_data <- BaseSamp_input %>%
  mutate(
    post = as.numeric(wk2open >= 0),
    TP = treat * post
  )

# Replikation von ReplicaCodes/Tab3.do
tab3_mod1 <- feols(
  lnspd_res ~ TP + treat | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc],
  data = tab3_data %>% filter(between(wk2open, -6, 47)),
  cluster = ~case
)

tab3_mod2 <- feols(
  lnspd_res ~ TP | linkid + yrwk,
  data = tab3_data %>% filter(treat == 1, between(wk2open, -6, 47)),
  cluster = ~case
)

tab3_mod3 <- feols(
  lnspd_res ~ TP | linkid + yrwk + yrwk[lnpop] + yrwk[lngdppc],
  data = tab3_data %>% filter(treat == 1, between(wk2open, -6, 47)),
  cluster = ~case
)

# Ausgabeformate für Koeffizienten, Standardfehler und Fallzahlen
fmt_coef <- function(m) sprintf("%.3f", coef(m)[["TP"]])
fmt_se <- function(m) sprintf("(%.3f)", sqrt(vcov(m)[["TP", "TP"]]))
fmt_n <- function(m) formatC(nobs(m), format = "d", big.mark = ",")

# Ergebnistabelle für Table 3 zusammenbauen
table3 <- tibble::tibble(
  term = c(
    "Treated × post",
    "",
    "Model",
    "Group-by-week-to-open fixed effects",
    "Road segment fixed effects",
    "Calendar week fixed effects",
    "Adjusted for differential seasonality",
    "Including segments from control cities",
    "",
    "Observations"
  ),
  `1` = c(fmt_coef(tab3_mod1), fmt_se(tab3_mod1), "Stack DID", "✓", "✓", "", "✓", "✓", "", fmt_n(tab3_mod1)),
  `2` = c(fmt_coef(tab3_mod2), fmt_se(tab3_mod2), "Two-way fixed effects", "", "✓", "✓", "", "", "", fmt_n(tab3_mod2)),
  `3` = c(fmt_coef(tab3_mod3), fmt_se(tab3_mod3), "Two-way fixed effects", "", "✓", "✓", "✓", "", "", fmt_n(tab3_mod3))
)

# Table 3 als HTML-Tabelle ausgeben
kableExtra::kbl(
  table3,
  format = "html",
  align = c("l", "c", "c", "c"),
  col.names = c("", "<span>(1)</span>", "<span>(2)</span>", "<span>(3)</span>"),
  escape = FALSE,
  caption = "Table 3—Baseline Estimates",
  table.attr = 'class="table3-original replicated-original-table"'
) %>%
  kableExtra::kable_styling(
    full_width = FALSE,
    position = "center",
    bootstrap_options = NULL,
    font_size = 18,
    html_font = "Times New Roman"
  ) %>%
  kableExtra::footnote(
    general = paste0(
      "The dependent variable is weekly average residual speed. ",
      "Standard errors are in parentheses. ",
      "This table is computed by replicating the estimation logic in ReplicaCodes/Tab3.do ",
      "directly in R (same sample restrictions and FE structure)."
    ),
    general_title = "Notes:",
    footnote_as_chunk = TRUE,
    escape = FALSE
  )
```

*Grafik selbst erstellt in Anlehnung an Table 3 aus Gu et. al (2021).*

```{css, echo=FALSE}
/* Tabellenkopf von Table 3 zentrieren */
.table3-original thead th {
  text-align: center !important;
  font-weight: normal !important;
}
```

Table 3 fasst die Hauptergebnisse in einer statischen Regressionsspezifikation zusammen. Statt für jede Woche relativ zur Eröffnung einen eigenen Effekt zu schätzen, wird hier die gesamte Post-Treatment-Periode über den Koeffizienten `Treated × post` zusammengefasst. Dieser Koeffizient misst den durchschnittlichen Effekt einer U-Bahn-Eröffnung auf die residualisierte logarithmierte Straßengeschwindigkeit nach dem Treatment.

In der bevorzugten stacked-DID-Spezifikation in Spalte (1) beträgt der geschätzte Koeffizient 0.044. Das entspricht näherungsweise einem Anstieg der Straßengeschwindigkeit um 4,4 Prozent auf direkt betroffenen Straßenabschnitten. Der Standardfehler liegt bei 0.008, sodass der Effekt statistisch präzise geschätzt ist. Die Spezifikation enthält Straßenabschnitt-Fixed-Effects, group-by-week-to-opening Fixed Effects, eine Anpassung für unterschiedliche Saisonalität sowie Kontrollstraßen aus Städten ohne neue U-Bahn-Eröffnung im Untersuchungszeitraum.

Die Spalten (2) und (3) verwenden alternativ Two-way-Fixed-Effects-Modelle nur mit Straßenabschnitten aus Treatment-Städten. Beide Spezifikationen ergeben einen etwas kleineren, aber weiterhin positiven Effekt von 0.036, also ungefähr 3,6 Prozent. In Spalte (3) wird zusätzlich für unterschiedliche Saisonalität nach Stadtmerkmalen kontrolliert; der geschätzte Effekt bleibt unverändert. Das ist wichtig, weil viele U-Bahn-Linien am Jahresende eröffnet wurden und Verkehrsmuster rund um Chinese New Year stark saisonal geprägt sein können.

Insgesamt zeigen Figure 2 und Table 3 dasselbe Grundmuster: Nach der Eröffnung neuer U-Bahn-Linien steigt die Geschwindigkeit auf betroffenen Straßenabschnitten messbar an. Die dynamische Darstellung spricht zusätzlich dafür, dass dieser Anstieg zeitlich eng mit der Eröffnung zusammenfällt und nicht bereits lange vorher einsetzt. Die Baseline-Regression verdichtet dieses Ergebnis zu einem durchschnittlichen Effekt von etwa 3,6 bis 4,4 Prozent. Damit liefern die Autoren direkte Evidenz dafür, dass U-Bahn-Ausbau Straßenstau zumindest kurzfristig reduzieren kann.




# 6 Robustheit und Identifikationsprobleme

Die Hauptergebnisse sprechen für einen positiven Effekt neuer U-Bahn-Linien auf die Straßengeschwindigkeit. Für die kausale Interpretation reicht das Baseline-Ergebnis allein jedoch nicht aus. Entscheidend ist, ob der geschätzte Effekt tatsächlich durch die U-Bahn-Eröffnung entsteht oder durch andere zeitgleiche Entwicklungen, etwa saisonale Verkehrsmuster, Chinese New Year, Bauarbeiten oder allgemeine Trends in Treatment-Städten. Die folgenden Robustheitsanalysen prüfen deshalb vor allem drei Punkte: ob der Effekt zeitlich zur tatsächlichen Eröffnung passt, ob er auch bei längeren Pre-Periods bestehen bleibt und wie stark die Ergebnisse vom problematischen Treatment Timing rund um Chinese New Year abhängen.

### Placebo Opening Dates

##### Figure 3

```{r fig3_data, echo=FALSE, include=FALSE}
# Replikation von ReplicaCodes/Fig3.do (Teil II und III) direkt aus BaseSamp.dta:
# Für jeden Placebo-Zeitpunkt wird wie in Stata Dp = treat * (wk2open >= i)
# geschätzt. Der frühere Zwischenschritt über TablesFigures/Fig3_PlaceboWald.dta
# wird bewusst nicht mehr verwendet.
fig3_base <- BaseSamp_input %>%
  mutate(
    post = as.numeric(wk2open >= 0)
  )

fig3_min_wk <- min(fig3_base$wk2open, na.rm = TRUE)
fig3_max_wk <- max(fig3_base$wk2open, na.rm = TRUE)
fig3_placebo_weeks <- seq(fig3_min_wk + 1, fig3_max_wk - 1)

fig3_df <- map_dfr(seq_along(fig3_placebo_weeks), function(n) {
  placebo_week <- fig3_placebo_weeks[n]
  fig3_model <- feols(
    lnspd_res ~ Dp + post + treat | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc],
    cluster = ~case,
    data = fig3_base %>%
      mutate(Dp = treat * as.numeric(wk2open >= placebo_week))
  )

  tibble(
    wk2open = n - 55,
    wald_stat = coef(fig3_model)[["Dp"]] / sqrt(vcov(fig3_model)["Dp", "Dp"])
  )
}) %>%
  filter(between(wk2open, -47, 47))
```

```{r fig3_plot, echo=FALSE, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 3 aus Gu et. al (2021)'}
# Darstellung analog zum Stata-Graphen in Fig3.do
p_fig3 <- ggplot(fig3_df, aes(x = wk2open, y = wald_stat)) +
  geom_hline(yintercept = 1:5, color = "#B0B4B8", linewidth = 0.8) +
  geom_point(shape = 4, color = "#5f7f77", size = 0.9, stroke = 1.35) +
  geom_vline(xintercept = 0, color = "red", linewidth = 0.9) +
  scale_x_continuous(
    breaks = seq(-48, 48, by = 4),
    limits = c(-48, 48)
  ) +
  scale_y_continuous(
    breaks = 0:5,
    limits = c(0, 5.4),
    expand = expansion(mult = c(0, 0.02))
  ) +
  labs(
    x = "Week relative to opening",
    y = "Wald statistic"
  ) +
  theme_classic(base_size = 16) +
  theme(
    panel.grid = element_blank(),
    plot.background = element_rect(fill = "white", color = NA),
    panel.background = element_rect(fill = "white", color = NA),
    axis.line = element_line(color = "#3A3A3A", linewidth = 0.7),
    axis.ticks = element_line(color = "#3A3A3A", linewidth = 0.6),
    axis.text.x = element_text(angle = 45, hjust = 1)
  )

p_fig3
```

Figure 3 untersucht, ob der geschätzte Effekt tatsächlich rund um die reale U-Bahn-Eröffnung auftritt oder ob er auch bei künstlich verschobenen Eröffnungszeitpunkten sichtbar wäre. Dafür wird das Modell wiederholt mit Placebo Opening Dates geschätzt, also mit fiktiven Treatment-Zeitpunkten vor und nach der tatsächlichen Eröffnung. Für jede dieser Schätzungen wird die Wald-Statistik des zentralen Treatment-Koeffizienten dargestellt.

Die Logik ist einfach: Wenn der Effekt nur durch allgemeine Trends oder saisonale Bewegungen getrieben wäre, müsste die stärkste Evidenz nicht zwingend genau beim echten Eröffnungszeitpunkt liegen. In Figure 3 liegt die Wald-Statistik jedoch klar am höchsten in der Nähe von Woche 0, also beim tatsächlichen Opening Date. Danach fällt sie deutlich ab. Das spricht dafür, dass der geschätzte Effekt zeitlich eng mit der U-Bahn-Eröffnung verbunden ist.

Der Test ist allerdings keine vollständige Lösung aller Identifikationsprobleme. Er zeigt vor allem, dass der Effekt nicht beliebig im Zeitverlauf auftaucht. Er kann aber nicht vollständig ausschließen, dass andere Ereignisse ebenfalls genau mit der U-Bahn-Eröffnung zusammenfallen, etwa das Ende von Bauarbeiten, Änderungen im Busnetz oder veränderte Verkehrsführungen rund um neue Stationen. Der Placebo-Test stärkt daher die Interpretation der Ergebnisse, ersetzt aber keine inhaltliche Diskussion möglicher zeitgleicher Confounder.

### Längere Pre-Period und Difference-in-Discontinuity

##### Table 4

```{r table4_varying_preperiod, results='asis', echo=FALSE}
# Trendvariablen für verschiedene Pre-Period-Spezifikationen erzeugen
tab4_data <- BaseSamp_input %>%
  mutate(
    post = as.numeric(wk2open >= 0),
    TP = treat * post,
    wkabs = abs(wk2open),
    wk2open_pre_treat = wkabs * (post == 0) * treat,
    wk2open_post_treat = wkabs * (post == 1) * treat,
    wk2open2_pre_treat = wkabs^2 * (post == 0) * treat,
    wk2open2_post_treat = wkabs^2 * (post == 1) * treat,
    wk2open3_pre_treat = wkabs^3 * (post == 0) * treat,
    wk2open3_post_treat = wkabs^3 * (post == 1) * treat,
    wk2open4_pre_treat = wkabs^4 * (post == 0) * treat,
    wk2open4_post_treat = wkabs^4 * (post == 1) * treat,
    wk2open5_pre_treat = wkabs^5 * (post == 0) * treat,
    wk2open5_post_treat = wkabs^5 * (post == 1) * treat
  )

# Eine Table-4-Spezifikation schätzen
run_tab4 <- function(data, pre_weeks = 48L, poly_degree = 0L) {
  data_use <- data %>% filter(between(wk2open, -pre_weeks, 47))
  rhs <- c("TP", "treat")
  if (poly_degree >= 1) {
    for (d in seq_len(poly_degree)) {
      if (d == 1) {
        rhs <- c(rhs, "wk2open_pre_treat", "wk2open_post_treat")
      } else {
        rhs <- c(
          rhs,
          paste0("wk2open", d, "_pre_treat"),
          paste0("wk2open", d, "_post_treat")
        )
      }
    }
  }
  fml <- as.formula(
    paste0(
      "lnspd_res ~ ", paste(rhs, collapse = " + "),
      " | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc]"
    )
  )
  feols(fml, cluster = ~case, data = data_use)
}

# Modelle mit unterschiedlichen Pre-Periods und Trends schätzen
tab4_mod1 <- run_tab4(tab4_data, pre_weeks = 12, poly_degree = 0)
tab4_mod2 <- run_tab4(tab4_data, pre_weeks = 24, poly_degree = 0)
tab4_mod3 <- run_tab4(tab4_data, pre_weeks = 48, poly_degree = 0)
tab4_mod4 <- run_tab4(tab4_data, pre_weeks = 48, poly_degree = 1)
tab4_mod5 <- run_tab4(tab4_data, pre_weeks = 48, poly_degree = 3)
tab4_mod6 <- run_tab4(tab4_data, pre_weeks = 48, poly_degree = 5)

tab4_models <- list(tab4_mod1, tab4_mod2, tab4_mod3, tab4_mod4, tab4_mod5, tab4_mod6)
fmt_coef <- function(m) sprintf("%.3f", coef(m)[["TP"]])
fmt_se <- function(m) sprintf("(%.3f)", sqrt(vcov(m)[["TP", "TP"]]))
fmt_n <- function(m) formatC(nobs(m), format = "d", big.mark = ",")

# Ergebnistabelle für Table 4 zusammenbauen
table4 <- tibble::tibble(
  term = c(
    "Treated × post",
    "",
    "Range of weeks relative to opening",
    "Polynomial of time trends",
    "",
    "Observations"
  ),
  `1` = c(fmt_coef(tab4_mod1), fmt_se(tab4_mod1), "[-12, 47]", "None", "", fmt_n(tab4_mod1)),
  `2` = c(fmt_coef(tab4_mod2), fmt_se(tab4_mod2), "[-24, 47]", "None", "", fmt_n(tab4_mod2)),
  `3` = c(fmt_coef(tab4_mod3), fmt_se(tab4_mod3), "[-48, 47]", "None", "", fmt_n(tab4_mod3)),
  `4` = c(fmt_coef(tab4_mod4), fmt_se(tab4_mod4), "[-48, 47]", "Linear", "", fmt_n(tab4_mod4)),
  `5` = c(fmt_coef(tab4_mod5), fmt_se(tab4_mod5), "[-48, 47]", "Up to third", "", fmt_n(tab4_mod5)),
  `6` = c(fmt_coef(tab4_mod6), fmt_se(tab4_mod6), "[-48, 47]", "Up to fifth", "", fmt_n(tab4_mod6))
)

# Table 4 als HTML-Tabelle ausgeben
kbl(
  table4,
  format = "html",
  align = c("l", rep("c", 6)),
  col.names = c("", "(1)", "(2)", "(3)", "(4)", "(5)", "(6)"),
  escape = FALSE,
  caption = "Table 4—Varying the Length of the Pre-Period",
  table.attr = 'class="table4-original replicated-original-table"'
) %>%
  add_header_above(c(" " = 1, "Length of pre-periods\nNumber of weeks before launch" = 3, "Discontinuity around the launch\nTreatment-specific time trend polynomial" = 3)) %>%
  add_header_above(c(" " = 1, "12" = 1, "24" = 1, "48" = 1, "Linear" = 1, "Up to third" = 1, "Up to fifth" = 1)) %>%
  kable_styling(
    full_width = FALSE,
    position = "center",
    bootstrap_options = NULL,
    font_size = 18,
    html_font = "Times New Roman"
  ) %>%
  footnote(
    general = paste0(
      "The dependent variable is a segment's weekly average log residual speed. ",
      "All models are estimated using the stacked DID model and include all 45 treated lines. ",
      "Polynomial of flexible time trends indicates the degree of the polynomials for treatment-specific ",
      "\"pre\" and \"post\" time trends. Standard errors are in parentheses, clustered at the group level."
    ),
    general_title = "Notes:",
    footnote_as_chunk = TRUE,
    escape = FALSE
  )
```

*Grafik selbst erstellt in Anlehnung an Table 4 aus Gu et. al (2021).*

```{css, echo=FALSE}
/* Tabellenkopf von Table 4 zentrieren */
.table4-original thead th {
  text-align: center !important;
  font-weight: normal !important;
}
```



Table 4 prüft, ob die Ergebnisse davon abhängen, dass die Baseline-Spezifikation nur die letzten sechs Wochen vor der Eröffnung als Pre-Period verwendet. Das ist relevant, weil eine kurze Pre-Period zwar Bauarbeiten kurz vor der Eröffnung ausblenden soll, aber gleichzeitig weniger Informationen über mögliche längerfristige Trends liefert. Die Autoren wiederholen die Schätzung deshalb mit längeren Pre-Periods von 12, 24 und 48 Wochen.

Die Ergebnisse bleiben in diesen Spezifikationen sehr ähnlich. Der geschätzte Effekt liegt bei 0.043, 0.039 und 0.039 und damit nahe am Baseline-Ergebnis. Das spricht dagegen, dass der Haupteffekt nur durch die enge Wahl des Pre-Treatment-Fensters entsteht. Gleichzeitig zeigt der zweite Teil der Tabelle Difference-in-Discontinuity-Spezifikationen mit flexiblen Treatment-spezifischen Zeittrends. Auch hier bleibt der Effekt positiv, fällt aber bei flexibleren Trends kleiner aus: von 0.042 bei linearem Trend auf 0.028 beziehungsweise 0.023 bei Polynomen höherer Ordnung. Das ist grundsätzlich beruhigend, zeigt aber auch, dass die genaue Effektgröße nicht völlig unabhängig von der Modellierung zeitlicher Trends ist.

##### Figure 4

```{r fig4_data, echo=FALSE, include=FALSE}
# Replikation von ReplicaCodes/Fig4.do
fig4_base <- BaseSamp_input %>%
  filter(between(wk2open, -48, 47))

# Event-Time-Dummies wie im Stata-Code: Am48-Am2, A0, Ap1-Ap47
for (w in -48:-2) {
  nm <- paste0("Am", abs(w))
  fig4_base[[nm]] <- fig4_base$treat * as.numeric(fig4_base$wk2open == w)
}
fig4_base$A0 <- fig4_base$treat * as.numeric(fig4_base$wk2open == 0)
for (w in 1:47) {
  nm <- paste0("Ap", w)
  fig4_base[[nm]] <- fig4_base$treat * as.numeric(fig4_base$wk2open == w)
}

rhs_fig4 <- c(
  paste0("Am", 48:2),
  "A0",
  paste0("Ap", 1:47),
  "treat"
)

form_fig4 <- as.formula(
  paste0(
    "lnspd_res ~ ",
    paste(rhs_fig4, collapse = " + "),
    " | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc]"
  )
)

mod_fig4 <- feols(form_fig4, cluster = ~case, data = fig4_base)

wk_seq <- -48:47
coef_val <- rep(NA_real_, length(wk_seq))
se_val <- rep(NA_real_, length(wk_seq))

for (i in seq_along(wk_seq)) {
  w <- wk_seq[i]
  if (w <= -2) {
    nm <- paste0("Am", abs(w))
    coef_val[i] <- unname(coef(mod_fig4)[nm])
    se_val[i] <- sqrt(vcov(mod_fig4)[nm, nm])
  } else if (w == -1) {
    coef_val[i] <- 0
    se_val[i] <- 0
  } else if (w == 0) {
    coef_val[i] <- unname(coef(mod_fig4)["A0"])
    se_val[i] <- sqrt(vcov(mod_fig4)["A0", "A0"])
  } else {
    nm <- paste0("Ap", w)
    coef_val[i] <- unname(coef(mod_fig4)[nm])
    se_val[i] <- sqrt(vcov(mod_fig4)[nm, nm])
  }
}

fig4_df <- tibble(
  wk2open = wk_seq,
  coef = coef_val,
  se = se_val,
  lo = coef - 1.96 * se,
  hi = coef + 1.96 * se
)
```

```{r fig4_plot, echo=FALSE, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 4 aus Gu et. al (2021)'}
# Darstellung analog zu Figure 4 (Longer Pre-Treatment Periods)
p_fig4 <- ggplot(fig4_df, aes(x = wk2open, y = coef)) +
  geom_hline(yintercept = c(-0.05, 0, 0.05, 0.10), color = "#B0B4B8", linewidth = 0.6) +
  geom_errorbar(aes(ymin = lo, ymax = hi), width = 0.6, color = "#6d8d84", linewidth = 0.7) +
  geom_point(shape = 16, size = 2.3, color = "#6d8d84") +
  geom_vline(xintercept = 0, color = "#90343a", linewidth = 0.8) +
  scale_x_continuous(breaks = seq(-48, 48, by = 4), limits = c(-48, 48)) +
  scale_y_continuous(
    breaks = c(-0.05, 0, 0.05, 0.10),
    limits = c(-0.065, 0.105),
    labels = c("-0.05", "0", "0.05", "0.1")
  ) +
  labs(x = "Weeks to subway opening", y = "Coefficient") +
  theme_classic(base_size = 16) +
  theme(
    panel.grid = element_blank(),
    plot.background = element_rect(fill = "white", color = NA),
    panel.background = element_rect(fill = "white", color = NA),
    axis.line = element_line(color = "#3A3A3A", linewidth = 0.7),
    axis.ticks = element_line(color = "#3A3A3A", linewidth = 0.6),
    axis.text.x = element_text(angle = 45, hjust = 1)
  )

p_fig4
```

Figure 4 ergänzt diese Evidenz grafisch. Sie zeigt die dynamischen Koeffizienten für ein deutlich längeres Zeitfenster von bis zu 48 Wochen vor und nach der U-Bahn-Eröffnung. Vor der Eröffnung schwanken die Koeffizienten überwiegend um null und zeigen keinen klaren positiven Vortrend. Direkt nach der Eröffnung steigen die Koeffizienten an und bleiben in der Post-Period überwiegend positiv.

Diese Darstellung ist wichtig für die Parallel-Trends-Annahme. Wenn die behandelten Straßenabschnitte bereits lange vor der Eröffnung systematisch schneller geworden wären, wäre die kausale Interpretation problematisch. Figure 4 liefert dafür keine starke Evidenz. Gleichzeitig bleiben die Konfidenzintervalle teilweise breit, sodass die Grafik eher als unterstützender Robustheitscheck zu verstehen ist und nicht als endgültiger Beweis perfekter Paralleltrends.

### Das CNY-Problem

##### Table 5

```{r table5_subsamples_launch, results='asis', echo=FALSE, message=FALSE,warning=FALSE}
# Replikation von ReplicaCodes/Tab5.do
# Laut Replication README ist BaseSamp.dta der Basissample-Datensatz für die meisten Hauptergebnisse.
tab5_data <- BaseSamp_input %>%
  mutate(
    post = as.numeric(wk2open >= 0),
    TP = treat * post,
    early_lines = as.numeric(opendate <= as.Date("2017-01-31")),
    mid_lines = as.numeric(opendate > as.Date("2017-01-31") & opendate <= as.Date("2017-11-30")),
    late_lines = as.numeric(opendate > as.Date("2017-11-30"))
  )

tab5_subset <- function(data, group = "all") {
  switch(
    group,
    all = data %>% filter(between(wk2open, -6, 3)),
    early = data %>% filter(early_lines == 1, between(wk2open, -6, 47)),
    mid = data %>% filter(mid_lines == 1, between(wk2open, -20, 20)),
    late = data %>% filter(late_lines == 1, between(wk2open, -47, 3))
  )
}

estimate_tab5 <- function(data_use, outcome = "lnspd_res", cluster = TRUE) {
  tab5_formula <- as.formula(
    paste0(outcome, " ~ TP + treat | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc]")
  )

  feols(
    tab5_formula,
    cluster = if (cluster) ~case else NULL,
    data = data_use
  )
}

run_tab5 <- function(data, group = "all", outcome = "lnspd_res", cluster = TRUE) {
  estimate_tab5(
    tab5_subset(data, group),
    outcome = outcome,
    cluster = cluster
  )
}

tab5_bootstrap_weights <- function(block_ids, reps = 499, seed = 190512) {
  set.seed(seed)
  weights <- replicate(
    reps,
    sample(c(-1, 1), length(block_ids), replace = TRUE)
  )
  rownames(weights) <- as.character(block_ids)
  weights
}

tab5_wild_block_bootstrap <- function(data, group, weights = NULL, n_blocks = NULL, reps = 499, seed = 190512) {
  # Wild block bootstrapping wie in ReplicaCodes/Tab5.do, aber ohne 499 langsame
  # feols-Neuschätzungen: Nach Frisch-Waugh-Lovell reicht es, TP einmal von den
  # Fixed Effects zu bereinigen. treat wird nicht separat aufgenommen, weil es in
  # diesen Spezifikationen von den Fixed Effects absorbiert wird.
  data_use <- tab5_subset(data, group)
  original_model <- estimate_tab5(data_use, cluster = FALSE)
  tp_partial_model <- feols(
    TP ~ 1 | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc],
    data = data_use
  )

  tp_residualized <- resid(tp_partial_model)
  fitted_values <- fitted(original_model)
  residual_values <- resid(original_model)
  stopifnot(length(tp_residualized) == nrow(data_use))

  tp_sum_squares <- sum(tp_residualized^2)
  original_numerator <- sum(tp_residualized * fitted_values)
  case_contributions <- tibble(
    case = as.character(data_use$case),
    contribution = tp_residualized * residual_values
  ) %>%
    group_by(case) %>%
    summarise(contribution = sum(contribution), .groups = "drop") %>%
    arrange(as.numeric(case))

  if (!is.null(n_blocks) && n_blocks > nrow(case_contributions)) {
    missing_blocks <- tibble(
      case = paste0("missing_", seq_len(n_blocks - nrow(case_contributions))),
      contribution = 0
    )
    case_contributions <- bind_rows(case_contributions, missing_blocks)
  }

  block_ids <- case_contributions$case
  case_weights <- if (is.null(weights)) {
    tab5_bootstrap_weights(block_ids, reps = reps, seed = seed)
  } else {
    weights[block_ids, , drop = FALSE]
  }
  boot_numerators <- original_numerator +
    as.numeric(crossprod(case_contributions$contribution, case_weights))
  boot_coefs <- boot_numerators / tp_sum_squares

  # Das Stata-Skript hängt die Originalschätzung an die 499 Bootstrap-Ziehungen
  # an, bevor die Perzentile berechnet werden. Diese Logik bleibt erhalten,
  # ohne eine vorberechnete Bootstrap-Datei einzulesen.
  c(boot_coefs, coef(original_model)["TP"])
}

tab5_mod1 <- run_tab5(tab5_data, "all")
tab5_mod2 <- run_tab5(tab5_data, "early")
tab5_mod3 <- run_tab5(tab5_data, "mid")
tab5_mod4 <- run_tab5(tab5_data, "late")

tab5_line_counts <- c(all = 45, early = 21, mid = 9, late = 15)
tab5_weights <- tab5_bootstrap_weights(as.character(sort(unique(tab5_data$case))))
tab5_boot2 <- tab5_wild_block_bootstrap(tab5_data, "early", n_blocks = tab5_line_counts[["early"]])
tab5_boot3 <- tab5_wild_block_bootstrap(tab5_data, "mid", weights = tab5_weights)
tab5_boot4 <- tab5_wild_block_bootstrap(tab5_data, "late", weights = tab5_weights)

ci95 <- function(x) {
  # Stata _pctile uses order statistics rather than R's default interpolation.
  x_sorted <- sort(x[!is.na(x)])
  x_sorted[ceiling(length(x_sorted) * c(0.025, 0.975))]
}
ci2 <- ci95(tab5_boot2)
ci3 <- ci95(tab5_boot3)
ci4 <- ci95(tab5_boot4)

fmt_coef <- function(m) sprintf("%.3f", coef(m)[["TP"]])
fmt_se <- function(m) sprintf("(%.3f)", sqrt(vcov(m)[["TP", "TP"]]))
fmt_ci <- function(ci) sprintf("[%.3f, %.3f]", ci[1], ci[2])
fmt_n <- function(m) formatC(nobs(m), format = "d", big.mark = ",")
table5 <- tibble::tibble(
  term = c(
    "Treated × post",
    "",
    "Range of weeks relative to opening",
    "Number of treated lines",
    "",
    "Observations"
  ),
  `1` = c(fmt_coef(tab5_mod1), fmt_se(tab5_mod1), "[-6, 3]", tab5_line_counts[["all"]], "", fmt_n(tab5_mod1)),
  `2` = c(fmt_coef(tab5_mod2), fmt_ci(ci2), "[-6, 47]", tab5_line_counts[["early"]], "", fmt_n(tab5_mod2)),
  `3` = c(fmt_coef(tab5_mod3), fmt_ci(ci3), "[-20, 20]", tab5_line_counts[["mid"]], "", fmt_n(tab5_mod3)),
  `4` = c(fmt_coef(tab5_mod4), fmt_ci(ci4), "[-48, 3]", tab5_line_counts[["late"]], "", fmt_n(tab5_mod4))
)

kbl(
  table5,
  format = "html",
  align = c("l", rep("c", 4)),
  col.names = c("", "All lines<br>(1)", "Before 1/31/17<br>(2)", "2/1/17–11/30/17<br>(3)", "After 12/1/17<br>(4)"),
  escape = FALSE,
  caption = "Table 5—Subsamples by Time of Launch",
  table.attr = 'class="table5-original replicated-original-table"'
) %>%
  kable_styling(
    full_width = FALSE,
    position = "center",
    bootstrap_options = NULL,
    font_size = 18,
    html_font = "Times New Roman"
  ) %>%
  footnote(
    general = paste0(
      "The dependent variable is a segment's weekly average log residual speed. ",
      "All models are estimated using the stacked DID model. In column 1, standard errors are in parentheses, ",
      "clustered at the group level. In columns 2 to 4, numbers in brackets are 95 percent confidence intervals ",
      "from 499 repetitions of wild block bootstrapping."
    ),
    general_title = "Notes:",
    footnote_as_chunk = TRUE,
    escape = FALSE
  )
```

*Grafik selbst erstellt in Anlehnung an Table 5 aus Gu et. al (2021).*

```{css, echo=FALSE}
/* Tabellenkopf von Table 5 zentrieren */
.table5-original thead th {
  text-align: center !important;
  font-weight: normal !important;
}
```



Das zentrale Identifikationsproblem der Studie ist das Treatment Timing rund um Chinese New Year. Viele U-Bahn-Linien wurden am Jahresende eröffnet. Genau in dieser Zeit verändern sich Verkehrsmuster in chinesischen Städten stark, weil viele Menschen verreisen und wirtschaftliche Aktivität zurückgeht. Dadurch kann Straßengeschwindigkeit auch ohne neue U-Bahn-Linie steigen. Besonders problematisch ist, dass Treatment-Städte im Durchschnitt größer und wohlhabender sind als Kontrollstädte und deshalb möglicherweise stärker von solchen saisonalen Verkehrsrückgängen betroffen sind.

Table 5 adressiert dieses Problem, indem die Autoren die Linien nach Eröffnungszeitpunkt in Teilstichproben aufteilen. In der engen Spezifikation mit allen Linien und einem kurzen Fenster von sechs Wochen vor bis drei Wochen nach Eröffnung beträgt der Effekt 0.036. Für Linien, die vor dem 31. Januar 2017 eröffnet wurden, liegt der Effekt bei 0.050. Für Linien, die zwischen Februar und November 2017 eröffnet wurden, beträgt er 0.039. Dieser zweite Wert ist besonders wichtig, weil diese Gruppe weniger direkt vom Jahresend- und CNY-Problem betroffen ist. Für Linien nach dem 1. Dezember 2017 fällt der Effekt mit 0.022 kleiner aus und das Konfidenzintervall umfasst null.

##### Figure 5
```{r fig5_data, echo=FALSE, message=FALSE, warning=FALSE}
# Basissample für die Launch-Time-Subsamples übernehmen
fig5_base <- BaseSamp_input

# Event-Study für ein Figure-5-Panel schätzen
fit_fig5_panel <- function(data, wk_min, wk_max) {
  d <- data %>%
    filter(between(wk2open, wk_min, wk_max))

  for (w in wk_min:-2) {
    d[[paste0("Am", abs(w))]] <- d$treat * as.numeric(d$wk2open == w)
  }
  d$A0 <- d$treat * as.numeric(d$wk2open == 0)
  for (w in 1:wk_max) {
    d[[paste0("Ap", w)]] <- d$treat * as.numeric(d$wk2open == w)
  }

  rhs_terms <- c(paste0("Am", abs(wk_min):2), "A0", paste0("Ap", 1:wk_max), "post", "treat")
  form <- as.formula(
    paste0(
      "lnspd_res ~ ", paste(rhs_terms, collapse = " + "),
      " | linkid + case_wk2open + yrwk[lnpop] + yrwk[lngdppc]"
    )
  )

  mod <- feols(form, cluster = ~case, data = d %>% mutate(post = as.numeric(wk2open >= 0)))

  weeks <- wk_min:wk_max
  out <- tibble(week = weeks, estimate = NA_real_, std_error = NA_real_)

  for (i in seq_along(weeks)) {
    w <- weeks[i]
    if (w <= -2) {
      nm <- paste0("Am", abs(w))
      out$estimate[i] <- unname(coef(mod)[nm])
      out$std_error[i] <- sqrt(vcov(mod)[nm, nm])
    } else if (w == -1) {
      out$estimate[i] <- 0
      out$std_error[i] <- 0
    } else if (w == 0) {
      out$estimate[i] <- unname(coef(mod)["A0"])
      out$std_error[i] <- sqrt(vcov(mod)["A0", "A0"])
    } else {
      nm <- paste0("Ap", w)
      out$estimate[i] <- unname(coef(mod)[nm])
      out$std_error[i] <- sqrt(vcov(mod)[nm, nm])
    }
  }

  out %>%
    mutate(
      ci_low = estimate - 1.96 * std_error,
      ci_high = estimate + 1.96 * std_error
    )
}

# Drei Subsamples nach Eröffnungszeitpunkt schätzen
fig5_a_df <- fit_fig5_panel(
  fig5_base %>% filter(opendate <= as.Date("2017-01-31")),
  wk_min = -6,
  wk_max = 47
)
fig5_b_df <- fit_fig5_panel(
  fig5_base %>% filter(opendate > as.Date("2017-01-31") & opendate <= as.Date("2017-11-30")),
  wk_min = -20,
  wk_max = 20
)
fig5_c_df <- fit_fig5_panel(
  fig5_base %>% filter(opendate > as.Date("2017-11-30")),
  wk_min = -48,
  wk_max = 3
)

# Figure-5-Panel aus Schätzergebnissen zeichnen
plot_fig5_panel <- function(df, panel_title, x_breaks, x_limits, y_breaks) {
  y_pad <- 0.005
  y_lims <- range(c(df$ci_low, df$ci_high), na.rm = TRUE) + c(-y_pad, y_pad)

  ggplot(df, aes(x = week, y = estimate)) +
    geom_errorbar(aes(ymin = ci_low, ymax = ci_high), width = 0.5, color = "#6d8d84", linewidth = 0.55) +
    geom_point(color = "#6d8d84", size = 1.8) +
    geom_vline(xintercept = 0, color = "#c23b55", linewidth = 0.7) +
    scale_x_continuous(breaks = x_breaks, limits = x_limits) +
    scale_y_continuous(breaks = y_breaks, limits = y_lims) +
    labs(title = panel_title, x = "Weeks to subway opening", y = NULL) +
    theme_minimal(base_size = 11) +
    theme(
      plot.title = element_text(hjust = 0, face = "plain"),
      panel.grid.minor = element_blank(),
      axis.text.x = element_text(angle = 45, hjust = 1)
    )
}

# Einzelpanels für Figure 5 erstellen
p_fig5_a <- plot_fig5_panel(
  fig5_a_df,
  "Panel A. Lines launched before January 31, 2017",
  x_breaks = seq(-8, 48, 4),
  x_limits = c(-8, 48),
  y_breaks = seq(-0.05, 0.15, 0.05)
)

p_fig5_b <- plot_fig5_panel(
  fig5_b_df,
  "Panel B. Lines launched between February 1, 2017 and November 30, 2017",
  x_breaks = seq(-20, 20, 4),
  x_limits = c(-20, 20),
  y_breaks = seq(-0.10, 0.15, 0.05)
)

p_fig5_c <- plot_fig5_panel(
  fig5_c_df,
  "Panel C. Lines launched after December 1, 2017",
  x_breaks = seq(-48, 4, 4),
  x_limits = c(-48, 4),
  y_breaks = seq(-0.10, 0.05, 0.05)
)
```

```{r fig5_combined, echo=FALSE, fig.width=10, fig.height=7.5, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 5 aus Gu et. al (2021)'}
# Drei Panels zu einer Abbildung kombinieren
(
  (p_fig5_a | p_fig5_b) /
  (p_fig5_c | plot_spacer())
) +
  plot_layout(
    widths = c(1, 1),
    heights = c(1, 1)
  )
```

Figure 5 zeigt die entsprechenden dynamischen Effekte für die drei Eröffnungsgruppen. Panel A und Panel B liefern relativ klare positive Effekte nach der Eröffnung. Besonders Panel B ist für die Identifikation relevant, weil die dort betrachteten Eröffnungen außerhalb der stark problematischen Jahresendperiode liegen. Dass auch hier positive Effekte sichtbar sind, spricht gegen die Interpretation, dass das gesamte Hauptergebnis nur durch Chinese-New-Year-Saisonalität entsteht.

Panel C ist schwächer. Für die Linien nach dem 1. Dezember 2017 sind die Schätzungen weniger präzise und der Post-Treatment-Zeitraum ist sehr kurz. Außerdem liegt diese Gruppe genau in der problematischsten Phase des Jahres. Deshalb sollte diese Teilstichprobe nicht als starke Bestätigung gelesen werden. Sie zeigt eher die Grenze der Identifikation: Das Paper kann das CNY-Problem sichtbar adressieren und teilweise entschärfen, aber nicht vollständig eliminieren.

Insgesamt stärken die Robustheitsanalysen die Hauptergebnisse, weil der Effekt zeitlich zur Eröffnung passt, bei längeren Pre-Periods erhalten bleibt und auch außerhalb der kritischsten Jahresendperiode sichtbar ist. Die wichtigste Schwachstelle bleibt dennoch das Treatment Timing. Gerade weil viele Eröffnungen nahe an Chinese New Year liegen und Treatment- und Kontrollstädte strukturell unterschiedlich sind, hängt die kausale Interpretation stark davon ab, ob die Saisonalitätskontrollen ausreichend sind.


# 7 Heterogene Effekte und Mechanismen

##### Figure 6



```{r fig6_data_and_estimation, echo=FALSE, warning=FALSE, message=FALSE}
# Stata-ähnliche Hilfsfunktionen für Indikatoren definieren
fig6_stata_eq <- function(x, value) {
  as.numeric(!is.na(x) & x == value)
}

fig6_stata_in <- function(x, values) {
  as.numeric(!is.na(x) & x %in% values)
}

fig6_stata_inrange <- function(x, lower, upper) {
  as.numeric(!is.na(x) & x >= lower & x <= upper)
}

fig6_stata_inrange_to_missing <- function(x, lower) {
  as.numeric(is.na(x) | x >= lower)
}

# Heterogenitätsindikatoren im ExtendSample erzeugen
fig6_base <- ExtendSample_input %>%
  mutate(
    Dp_Roadtype12 = Dp * fig6_stata_in(roadtype, c(1, 2)),
    Dp_Roadtype3 = Dp * fig6_stata_eq(roadtype, 3),
    Dp_Roadtype4 = Dp * fig6_stata_eq(roadtype, 4),
    Dp_dist2line_0to1 = Dp * fig6_stata_inrange(link2_nearest_treat_line_km, 0, 1),
    Dp_dist2line_1to2p5 = Dp * fig6_stata_inrange(link2_nearest_treat_line_km, 1, 2.5),
    Dp_same_direct = Dp * same_direct,
    Dp_diff_direct = Dp * fig6_stata_eq(same_direct, 0),
    Dp_MostAffectedLinks = Dp * MostAffectedLinks,
    Dp_NonMostAffectedLinks = Dp * fig6_stata_eq(MostAffectedLinks, 0),
    Dp_MoreCongested = Dp * fig6_stata_eq(MoreCongested, 1),
    Dp_LessCongested = Dp * fig6_stata_eq(MoreCongested, 0),
    row_id_fig6 = row_number()
  )

# Tatsächlich genutzte Beobachtungen aus fixest-Modellen rekonstruieren
fig6_estimation_data <- function(model, data) {
  rows <- seq_len(nrow(data))

  if (!is.null(model$obs_selection)) {
    for (selection in model$obs_selection) {
      if (is.logical(selection)) {
        rows <- rows[selection]
      } else if (is.numeric(selection) && length(selection) > 0) {
        rows <- rows[selection]
      }
    }
  }

  data[rows, , drop = FALSE]
}

# Koeffizient, Konfidenzintervall und Stauindex für eine Zeile berechnen
fig6_coef_row <- function(model, term, y_position, ci_var, ci_filter, label, data_for_mean) {
  td <- broom::tidy(model)
  hit <- td %>% filter(term == !!term)
  est_data <- fig6_estimation_data(model, data_for_mean)

  tibble(
    y_position = y_position,
    label = label,
    estimate = hit$estimate[1],
    std_error = hit$std.error[1],
    avg_congestion = est_data %>%
      filter({{ ci_filter }}) %>%
      summarise(avg = mean(.data[[ci_var]], na.rm = TRUE)) %>%
      pull(avg)
  )
}

fig6_base_filter <- fig6_base %>%
  filter(link2_nearest_treat_line_km <= 2.5 | treat == 0)

mod_fig6_all <- feols(
  lnspd_res ~ Dp + treat | case_wk2open + case_linkid + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_base_filter
)

fig6_direct <- fig6_base_filter %>%
  mutate(
    hetero = if_else(MostAffectedLinks == 1, 1, 2, missing = 2),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE)
  )
mod_fig6_direct <- feols(
  lnspd_res ~ Dp_NonMostAffectedLinks + Dp_MostAffectedLinks + treat |
    case_wk2open + case_linkid + case_wk2open_hetero + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_direct
)

fig6_distance <- fig6_base %>%
  filter(link2_nearest_treat_line_km <= 2.5) %>%
  mutate(
    hetero = case_when(
      between(link2_nearest_treat_line_km, 0, 1) ~ 1,
      between(link2_nearest_treat_line_km, 1, 2.5) ~ 2,
      TRUE ~ NA_real_
    ),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE)
  )
mod_fig6_distance <- feols(
  lnspd_res ~ Dp_dist2line_0to1 + Dp_dist2line_1to2p5 + treat |
    case_wk2open + case_linkid + case_wk2open_hetero + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_distance
)

fig6_direction <- fig6_base_filter %>%
  mutate(
    hetero = case_when(
      same_direct == 1 ~ 1,
      same_direct == 0 ~ 2,
      TRUE ~ NA_real_
    ),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE)
  )
mod_fig6_direction <- feols(
  lnspd_res ~ Dp_same_direct + Dp_diff_direct + treat |
    case_wk2open + case_linkid + case_wk2open_hetero + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_direction
)

fig6_congestion <- fig6_base_filter %>%
  mutate(
    hetero = case_when(
      MoreCongested == 1 ~ 1,
      MoreCongested == 0 ~ 2,
      TRUE ~ NA_real_
    ),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE)
  )
mod_fig6_congestion <- feols(
  lnspd_res ~ Dp_MoreCongested + Dp_LessCongested + treat |
    case_wk2open + case_linkid + case_wk2open_hetero + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_congestion
)

fig6_roadtype <- fig6_base_filter %>%
  mutate(
    hetero = case_when(
      roadtype %in% c(1, 2) ~ 1,
      roadtype == 3 ~ 2,
      roadtype == 4 | is.na(roadtype) ~ 3,
      TRUE ~ NA_real_
    ),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE)
  )
mod_fig6_roadtype <- feols(
  lnspd_res ~ Dp_Roadtype12 + Dp_Roadtype3 + Dp_Roadtype4 + treat |
    case_wk2open + case_linkid + case_wk2open_hetero + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_roadtype
)

fig6_with_traffic <- fig6_base %>%
  inner_join(
    WithAgainstTraffic_input %>%
      select(case, linkid, wk2open, WithTraffic, lnspd_res_WithAgainstTraffic, CI_WithAgainstTraffic),
    by = c("case", "linkid", "wk2open")
  ) %>%
  filter(!is.na(WithTraffic), link2_nearest_treat_line_km <= 2.5 | treat == 0) %>%
  mutate(
    Dp_WithTraffic = Dp * fig6_stata_eq(WithTraffic, 1),
    Dp_AgainstTraffic = Dp * fig6_stata_eq(WithTraffic, 0),
    hetero = if_else(WithTraffic == 0, 1, 2),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE)
  )
mod_fig6_with_traffic <- feols(
  lnspd_res_WithAgainstTraffic ~ Dp_WithTraffic + Dp_AgainstTraffic + treat |
    case_wk2open + case_linkid + case_wk2open_hetero + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_with_traffic
)

fig6_rush <- fig6_base %>%
  select(-any_of(c("lnspd_res", "CI_res", "CI", "speed", "lnspd"))) %>%
  inner_join(
    RushNonrusHours_input %>%
      select(case, linkid, wk2open, rushhour, lnspd_res, CI),
    by = c("case", "linkid", "wk2open")
  ) %>%
  filter(!is.na(rushhour), link2_nearest_treat_line_km <= 2.5 | treat == 0) %>%
  mutate(
    Dp_Rush = Dp * fig6_stata_eq(rushhour, 1),
    Dp_NoRush = Dp * fig6_stata_eq(rushhour, 0),
    hetero = if_else(rushhour == 0, 1, 2),
    case_wk2open_hetero = interaction(case, wk2open, hetero, drop = TRUE),
    link_rushhour = interaction(linkid, rushhour, drop = TRUE)
  )
mod_fig6_rush <- feols(
  lnspd_res ~ Dp_Rush + Dp_NoRush + treat |
    case_linkid + case_wk2open_hetero + link_rushhour + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_rush
)

fig6_line_distance <- fig6_base %>%
  filter(link2_nearest_line_km <= 2.5 | treat == 0) %>%
  mutate(
    Dp_DistCat1 = Dp * fig6_stata_inrange(link2_nearest_treat_line_km, 0, 1),
    Dp_DistCat2 = Dp * fig6_stata_inrange(link2_nearest_treat_line_km, 1, 2.5),
    Dp_DistCat3 = Dp * fig6_stata_inrange_to_missing(link2_nearest_treat_line_km, 2.5) * fig6_stata_inrange(link2_nearest_line_km, 0, 1),
    Dp_DistCat4 = Dp * fig6_stata_inrange_to_missing(link2_nearest_treat_line_km, 2.5) * fig6_stata_inrange(link2_nearest_line_km, 1, 2.5)
  )
mod_fig6_line_distance <- feols(
  lnspd_res ~ Dp_DistCat1 + Dp_DistCat2 + Dp_DistCat3 + Dp_DistCat4 + treat |
    case_wk2open + case_linkid + yrwk[lnpop] + yrwk[lngdppc],
  cluster = ~case,
  data = fig6_line_distance
)

# Ergebnisse aller Figure-6-Spezifikationen sammeln
fig6_results <- bind_rows(
  fig6_coef_row(mod_fig6_all, "Dp", 1, "CI", TRUE, "All", fig6_base_filter),
  fig6_coef_row(mod_fig6_direct, "Dp_MostAffectedLinks", 3, "CI", MostAffectedLinks == 1, "Directly affected", fig6_direct),
  fig6_coef_row(mod_fig6_direct, "Dp_NonMostAffectedLinks", 4, "CI", MostAffectedLinks == 0, "Non-directly affected", fig6_direct),
  fig6_coef_row(mod_fig6_distance, "Dp_dist2line_0to1", 6, "CI", between(link2_nearest_treat_line_km, 0, 1), "d1 = [0, 1]", fig6_distance),
  fig6_coef_row(mod_fig6_distance, "Dp_dist2line_1to2p5", 7, "CI", between(link2_nearest_treat_line_km, 1, 2.5), "d1 = (1, 2.5]", fig6_distance),
  fig6_coef_row(mod_fig6_direction, "Dp_same_direct", 9, "CI", same_direct == 1, "Parallel", fig6_direction),
  fig6_coef_row(mod_fig6_direction, "Dp_diff_direct", 10, "CI", same_direct == 0, "Orthogonal", fig6_direction),
  fig6_coef_row(mod_fig6_congestion, "Dp_MoreCongested", 12, "CI", MoreCongested == 1, "More congested", fig6_congestion),
  fig6_coef_row(mod_fig6_congestion, "Dp_LessCongested", 13, "CI", MoreCongested == 0, "Less congested", fig6_congestion),
  fig6_coef_row(mod_fig6_roadtype, "Dp_Roadtype12", 15, "CI", roadtype %in% c(1, 2), "Highway/expressway", fig6_roadtype),
  fig6_coef_row(mod_fig6_roadtype, "Dp_Roadtype3", 16, "CI", roadtype == 3, "Arterial roads", fig6_roadtype),
  fig6_coef_row(mod_fig6_roadtype, "Dp_Roadtype4", 17, "CI", roadtype == 4, "Subarterial roads", fig6_roadtype),
  fig6_coef_row(mod_fig6_with_traffic, "Dp_WithTraffic", 19, "CI_WithAgainstTraffic", WithTraffic == 1, "With traffic", fig6_with_traffic),
  fig6_coef_row(mod_fig6_with_traffic, "Dp_AgainstTraffic", 20, "CI_WithAgainstTraffic", WithTraffic == 0, "Against traffic", fig6_with_traffic),
  fig6_coef_row(mod_fig6_rush, "Dp_Rush", 22, "CI", rushhour == 1, "Rush hour", fig6_rush),
  fig6_coef_row(mod_fig6_rush, "Dp_NoRush", 23, "CI", rushhour == 0, "Off-peak hour", fig6_rush),
  fig6_coef_row(mod_fig6_line_distance, "Dp_DistCat1", 25, "CI", between(link2_nearest_treat_line_km, 0, 1), "d1 = [0, 1]", fig6_line_distance),
  fig6_coef_row(mod_fig6_line_distance, "Dp_DistCat2", 26, "CI", between(link2_nearest_treat_line_km, 1, 2.5), "d1 = (1, 2.5]", fig6_line_distance),
  fig6_coef_row(mod_fig6_line_distance, "Dp_DistCat3", 27, "CI", link2_nearest_treat_line_km >= 2.5 & between(link2_nearest_line_km, 0, 1), "d1 = (2.5, .), d2 = [0, 1]", fig6_line_distance),
  fig6_coef_row(mod_fig6_line_distance, "Dp_DistCat4", 28, "CI", link2_nearest_treat_line_km >= 2.5 & between(link2_nearest_line_km, 1, 2.5), "d1 = (2.5, .), d2 = (1, .)", fig6_line_distance)
) %>%
  mutate(
    ci_low = estimate - 1.96 * std_error,
    ci_high = estimate + 1.96 * std_error,
    text_label = paste0(
      format(round(estimate, 3), nsmall = 3, trim = TRUE),
      " (",
      format(round(std_error, 3), nsmall = 3, trim = TRUE),
      ")        ",
      format(round(avg_congestion, 2), nsmall = 2, trim = TRUE)
    )
  )
```

```{r fig6_plot, echo=FALSE, fig.width=7.4, fig.height=7.4, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 6 aus Gu et. al (2021)'}
# Achsenlabels aus den Ergebniszeilen übernehmen
fig6_axis_labels <- fig6_results$label
names(fig6_axis_labels) <- -fig6_results$y_position

# Balkengrafik mit Konfidenzintervallen zeichnen
p_fig6 <- ggplot(fig6_results, aes(y = -y_position)) +
  geom_rect(aes(xmin = 0, xmax = estimate, ymin = -y_position - 0.39, ymax = -y_position + 0.39), fill = "#6d9996", color = "#2c5673", linewidth = 0.8) +
  geom_segment(aes(x = ci_low, xend = ci_high, yend = -y_position), color = "#9c2f34", linewidth = 0.65) +
  geom_segment(aes(x = ci_low, xend = ci_low, y = -y_position - 0.25, yend = -y_position + 0.25), color = "#9c2f34", linewidth = 0.65) +
  geom_segment(aes(x = ci_high, xend = ci_high, y = -y_position - 0.25, yend = -y_position + 0.25), color = "#9c2f34", linewidth = 0.65) +
  geom_vline(xintercept = 0, color = "black", linewidth = 0.55) +
  geom_hline(yintercept = -24, color = "#7f7f7f", linetype = "dashed", linewidth = 0.7) +
  geom_text(aes(x = 0.082, label = text_label), hjust = 0, size = 3.15, family = "serif") +
  annotate("text", x = 0.082, y = 0.35, label = "Coefficient (SE)   Average\n                     congestion index", hjust = 0, size = 3.15, family = "serif", lineheight = 0.9) +
  scale_y_continuous(
    breaks = c(0, -fig6_results$y_position),
    labels = c(" ", fig6_axis_labels),
    limits = c(-29.2, 1.2),
    expand = expansion(mult = c(0.01, 0.01))
  ) +
  scale_x_continuous(
    breaks = c(-0.01, 0, 0.05, 0.10, 0.15),
    labels = c("−0.01", "0", "0.05", "0.1", "0.15"),
    limits = c(-0.015, 0.153),
    expand = expansion(mult = c(0, 0))
  ) +
  labs(x = "Coefficient", y = NULL) +
  theme_minimal(base_family = "serif", base_size = 11) +
  theme(
    panel.grid.major.y = element_line(color = "#d2d2d2", linewidth = 0.45),
    panel.grid.major.x = element_blank(),
    panel.grid.minor = element_blank(),
    axis.line.x = element_line(color = "#333333", linewidth = 0.5),
    axis.line.y = element_line(color = "#333333", linewidth = 0.5),
    axis.text.y = element_text(color = "#222222", size = 9.6),
    axis.text.x = element_text(color = "#222222", size = 9.6),
    axis.title.x = element_text(margin = margin(t = 10), size = 11),
    plot.caption = element_text(hjust = 0.5, face = "bold", size = 10, margin = margin(t = 18)),
    plot.margin = margin(8, 6, 8, 6)
  )

p_fig6
```

Figure 6 untersucht, ob der Effekt neuer U-Bahn-Linien für unterschiedliche Straßenabschnitte gleich stark ausfällt. Das ist wichtig, weil ein überzeugender Mechanismus nicht nur einen durchschnittlichen Effekt zeigen sollte, sondern auch ein plausibles Muster: Wenn U-Bahnen tatsächlich Straßenverkehr ersetzen, sollte der Effekt vor allem dort sichtbar sein, wo die neue Linie eine realistische Alternative zur Autofahrt bietet und wo zuvor besonders starke Überlastung bestand.

Die Ergebnisse passen grundsätzlich zu dieser Substitutionslogik. Direkt betroffene Straßenabschnitte reagieren stärker als nicht direkt betroffene Straßen. Auch Straßen, die näher an der neuen U-Bahn-Linie liegen, zeigen größere Geschwindigkeitszuwächse als weiter entfernte Straßen. Besonders relevant ist außerdem der Unterschied nach ursprünglicher Überlastung: Auf anfangs stärker congested roads ist der Effekt deutlich größer als auf weniger überlasteten Straßen. Das spricht dafür, dass vor allem dort Verkehr entlastet wird, wo die Staukosten vorher besonders hoch waren und der Wechsel zur U-Bahn für Pendlerinnen und Pendler attraktiver ist.

Auch die weiteren Heterogenitäten stützen den Mechanismus. Der Effekt ist in der Rush Hour stärker als außerhalb der Stoßzeiten und tritt besonders auf Straßen auf, die mit dem Hauptverkehrsfluss belastet sind. Zudem finden die Autoren Hinweise auf Netzwerk- oder Spillover-Effekte: Auch Straßen, die nicht direkt an der neuen Linie liegen, aber näher an bestehenden U-Bahn-Linien sind, können profitieren. Insgesamt zeigt Figure 6 damit, dass der Effekt nicht zufällig über das Straßennetz verteilt ist, sondern dort stärker ausfällt, wo man ihn theoretisch erwarten würde.

# 8 Ökonomische Interpretation

##### Figure 7

Die Replikations-README ordnet Figure 7 keiner Stata-Datei und keinem Datensatz zu: Figure 7 ist eine theoretische Illustration des Wohlfahrtseffekts, deshalb gibt es laut README „no code associated with it“. Für die Rmd wird die Grafik daher direkt aus geometrischen Hilfsdaten erzeugt. Die Form orientiert sich an der im Paper gezeigten Illustration: Der rote Bereich `A` zeigt den Wohlfahrtsgewinn der weiterhin per Auto pendelnden Personen, der blau gefüllte Bereich `B` den Wohlfahrtsgewinn der Wechsler zur U-Bahn.

```{r fig7_data, echo=FALSE}
# Figure 7 ist nicht empirisch; entsprechend der Replication-README existieren
# keine Stata-Replikationsdateien und keine .dta-Daten für diese Abbildung.
# Die folgenden Datenpunkte sind daher reine Konstruktionskoordinaten für die
# theoretische Wohlfahrtsillustration.
fig7_x_star <- 0.42
fig7_y_axis <- 0.10
fig7_y_floor <- 0.00

fig7_road_slope <- 0.78
fig7_y_subway <- function(x) 0.82 - 0.70 * x
fig7_y_road <- function(x) 0.105 + fig7_road_slope * x
fig7_y_star <- fig7_y_subway(fig7_x_star)
# Die verschobene Autonutzenlinie verläuft parallel zur ursprünglichen
# Autonutzenlinie und geht durch den Schnittpunkt mit der U-Bahn-Linie.
fig7_road_shift <- fig7_y_star - fig7_y_road(fig7_x_star)
fig7_y_shifted_road <- function(x) fig7_y_road(x) + fig7_road_shift

fig7_left_area <- tibble(
  x = c(0, fig7_x_star, 0),
  y = c(fig7_y_shifted_road(0), fig7_y_star, fig7_y_subway(0))
)

fig7_right_area <- tibble(
  x = c(fig7_x_star, 1, 1, fig7_x_star),
  y = c(fig7_y_star, fig7_y_shifted_road(1), fig7_y_road(1), fig7_y_road(fig7_x_star))
)

fig7_curve_data <- tibble(x = seq(0, 1, length.out = 200)) %>%
  mutate(
    subway = fig7_y_subway(x),
    road = fig7_y_road(x),
    shifted_road = fig7_y_shifted_road(x)
  )
```

```{r fig7_plot, echo=FALSE, fig.width=7.2, fig.height=5.7, fig.cap='Grafik selbst erstellt in Anlehnung an Figure 7 aus Gu et. al (2021)'}
# Theoretische Wohlfahrtsgrafik zeichnen
p_fig7 <- ggplot() +
  geom_polygon(
    data = fig7_left_area,
    aes(x = x, y = y),
    fill = "#9bd6f5",
    color = NA,
    alpha = 0.82
  ) +
  geom_polygon(
    data = fig7_right_area,
    aes(x = x, y = y),
    fill = "#c98b7d",
    color = NA,
    alpha = 0.96
  ) +
  geom_line(
    data = fig7_curve_data,
    aes(x = x, y = subway),
    color = "#6b6f72",
    linewidth = 0.9
  ) +
  geom_line(
    data = fig7_curve_data,
    aes(x = x, y = road),
    color = "#6b6f72",
    linewidth = 0.9
  ) +
  geom_line(
    data = fig7_curve_data,
    aes(x = x, y = shifted_road),
    color = "#1d252b",
    linewidth = 1.65,
    linetype = "11"
  ) +
  geom_segment(aes(x = 0, xend = 1, y = fig7_y_axis, yend = fig7_y_axis), color = "#6b6f72", linewidth = 0.75) +
  geom_segment(aes(x = 0, xend = 0, y = fig7_y_floor, yend = 1), color = "#1f1f1f", linewidth = 0.8) +
  geom_segment(aes(x = 1, xend = 1, y = fig7_y_floor, yend = 1), color = "#1f1f1f", linewidth = 0.8) +
  geom_segment(aes(x = 0, xend = 0, y = fig7_y_floor, yend = fig7_y_axis), color = "#6b6f72", linewidth = 0.9, linetype = "42") +
  geom_segment(aes(x = fig7_x_star, xend = fig7_x_star, y = fig7_y_floor, yend = fig7_y_star), color = "#6b6f72", linewidth = 0.9, linetype = "42") +
  geom_segment(aes(x = 1, xend = 1, y = fig7_y_floor, yend = fig7_y_axis), color = "#6b6f72", linewidth = 0.9, linetype = "42") +
  geom_segment(aes(x = 0, xend = 1, y = fig7_y_floor, yend = fig7_y_floor), color = "#6b6f72", linewidth = 0.9, linetype = "42") +
  geom_segment(aes(x = 0.08, xend = 0.30, y = 0.075, yend = 0.075), arrow = arrow(length = unit(0.13, "cm"), type = "closed"), linewidth = 0.45, color = "black") +
  geom_segment(aes(x = 0.96, xend = 0.74, y = 0.075, yend = 0.075), arrow = arrow(length = unit(0.13, "cm"), type = "closed"), linewidth = 0.45, color = "black") +
  geom_segment(
  aes(
    x = 0.88,
    xend = 0.86,
    y = fig7_y_road(0.86) - 0.07,
    yend = fig7_y_road(0.86)
  ),
  color = "#6b6f72",
  linewidth = 0.8,
  linetype = "42"
  ) +
  geom_segment(
    aes(
      x = 0.78,
      xend = 0.90,
      y = 0.88,
      yend = fig7_y_shifted_road(0.90)
    ),
  color = "#6b6f72",
  linewidth = 0.8,
  linetype = "42"
  ) +
  annotate("text", x = 0.60, y = 0.64, label = "A", family = "serif", fontface = "italic", size = 4.5) +
  annotate("text", x = 0.19, y = 0.47, label = "B", family = "serif", fontface = "italic", size = 4.5) +
  annotate("text", x = 0.09, y = 0.72, label = "u[s] + xi[is]", parse = TRUE, family = "serif", size = 3.4) +
  annotate("text", x = 0.89, y = 0.68, label = "u[a] + xi[ia]", parse = TRUE, family = "serif", size = 3.6) +
  annotate("text", x = 0.71, y = 0.88, label = "u[a] + Delta*u[a] + xi[ia]", parse = TRUE, family = "serif", size = 3.4)+
  annotate("text", x = 0.19, y = -0.05, label = "N[s]", parse = TRUE, family = "serif", size = 3.6) +
  annotate("text", x = 0.86, y = -0.05, label = "N[a]", parse = TRUE, family = "serif", size = 3.6) +
  annotate("text",x = 0.19,y = 0.04,label = "xi[is] * ' ↑ relative to ' * xi[ia]",parse = TRUE,family = "serif",size = 3.25) +
  annotate("text",x = 0.86,y = 0.04,label = "xi[ia] * ' ↑ relative to ' * xi[is]",parse = TRUE,family = "serif",size = 3.25) +
  
  labs(
    x = NULL,
    y = NULL
  ) +
  coord_cartesian(xlim = c(0, 1), ylim = c(0, 1), clip = "off") +
  theme_void(base_family = "serif") +
  theme(
    plot.caption = element_text(hjust = 0.5, face = "bold", size = 10, margin = margin(t = 15)),
    plot.margin = margin(8, 22, 8, 22)
  )

p_fig7
```

```{r fig7_save, include=FALSE}
# Figure 7 als PNG speichern
ggsave(
  filename = "FertigeGrafiken/Fig7_R.png",
  plot = p_fig7,
  width = 7.2,
  height = 5.7,
  dpi = 300
)
```

Figure 7 übersetzt den empirischen Geschwindigkeitseffekt in eine ökonomische Wohlfahrtslogik. Die Grundidee ist, dass eine neue U-Bahn-Linie zwei Gruppen betrifft. Erstens gibt es Personen, die vom Auto oder Bus zur U-Bahn wechseln. Zweitens gibt es Personen, die weiterhin auf der Straße unterwegs sind, aber durch weniger Stau schneller fahren können. Der empirisch geschätzte DID-Effekt bezieht sich vor allem auf diese zweite Gruppe: Er misst, wie stark sich die Straßengeschwindigkeit nach der U-Bahn-Eröffnung erhöht.

In der Grafik entspricht der Nutzen der verbleibenden Straßennutzerinnen und Straßennutzer der Fläche A. Diese Gruppe gewinnt, weil weniger Verkehr auf der Straße ist und dadurch Reisezeit eingespart wird. Die Fläche B beschreibt den Nutzen der Personen, die zur U-Bahn wechseln. Ob diese Gruppe gewinnt, hängt davon ab, wie sich Reisezeit, monetäre Kosten, Komfort und individuelle Präferenzen zwischen Straße und U-Bahn unterscheiden. Zusätzlich müssen staatliche Ausgaben berücksichtigt werden, etwa Baukosten, Betriebskosten und Subventionen für den öffentlichen Verkehr.

Für Beijing berechnen Gu et al. (2021), dass die reine Zeitersparnis durch höhere Straßengeschwindigkeit pro Auto- oder Buspendelfahrt etwa 0,10 US-Dollar wert ist. Auf aggregierter Ebene ergibt sich daraus ein relevanter Wohlfahrtsgewinn, weil sehr viele Pendelfahrten betroffen sind. Diese Rechnung macht deutlich, dass selbst relativ kleine prozentuale Geschwindigkeitssteigerungen ökonomisch bedeutsam sein können, wenn sie in großen Städten auf Millionen von Fahrten wirken.

# 9 Diskussion und Kritik

Die Studie liefert überzeugende Evidenz dafür, dass neue U-Bahn-Linien kurzfristig die Geschwindigkeit auf betroffenen Straßen erhöhen. Trotzdem bleibt die kausale Interpretation nicht vollständig unproblematisch. Die wichtigsten Kritikpunkte betreffen das Treatment Timing, die Wahl der Kontrollstädte, die Messung über Baidu Maps, die externe Validität sowie die Berechnung des Welfare Impacts.

### Treatment Timing

Das größte Identifikationsproblem liegt im Timing der U-Bahn-Eröffnungen. Viele Linien wurden am Jahresende eröffnet, also in zeitlicher Nähe zu Chinese New Year. Genau in dieser Phase verändern sich Verkehrsmuster in chinesischen Städten stark, weil viele Menschen die großen Städte verlassen und wirtschaftliche Aktivität zurückgeht. Dadurch kann die Straßengeschwindigkeit auch ohne neue U-Bahn-Linie steigen.

Die mögliche Bias-Richtung ist hier relativ klar: Wenn die Treatment-Städte rund um Chinese New Year ohnehin einen stärkeren Verkehrsrückgang erleben als die Kontrollstädte, wird der U-Bahn-Effekt überschätzt. Die Autoren adressieren dieses Problem mit Saisonalitätskontrollen, Placebo-Tests und Teilstichproben nach Eröffnungszeitpunkt. Diese Robustheitschecks schwächen den Einwand ab, beseitigen ihn aber nicht vollständig. Gerade weil die Eröffnungsdaten administrativ gesetzt werden und stark im Dezember clustern, bleibt Treatment Timing die zentrale Schwachstelle der Studie.

### Kontrollstädte

Ein zweites Problem betrifft die Kontrollgruppe. Die Kontrollstädte sind keine perfekten Gegenfakten für die Treatment-Städte. Das Paper zeigt selbst, dass Treatment-Städte im Durchschnitt deutlich größer und etwas wohlhabender sind als Kontrollstädte. Diese Unterschiede sind nicht nur deskriptiv relevant, sondern können direkt die Identifikation beeinflussen. Größere und reichere Städte haben andere Pendelstrukturen, andere Verkehrsdichte, andere Migrationsmuster und möglicherweise auch andere saisonale Schwankungen.

Die Bias-Richtung ist hier weniger eindeutig als beim CNY-Problem. Wenn größere Städte um Chinese New Year stärker entlastet werden, könnte der Effekt überschätzt werden. Wenn Kontrollstädte dagegen grundsätzlich andere Verkehrstrends oder schlechtere Substitutionsmöglichkeiten haben, könnte der Vergleich auch in andere Richtungen verzerrt sein. Die Fixed Effects und Saisonalitätskontrollen helfen, aber sie können nur beobachtbare oder zeitinvariante Unterschiede kontrollieren. Ein Restrisiko bleibt, dass die Kontrollstädte nicht das passende kontrafaktische Verkehrsmuster für die Treatment-Städte abbilden.

### Messung durch Baidu Maps

Die Nutzung von Baidu-Maps-Daten ist eine große Stärke der Studie, weil sie sehr hochfrequente und räumlich granulare Verkehrsinformationen ermöglicht. Gleichzeitig ist die Messung nicht perfekt. Die Daten basieren auf mobilen Geräten mit aktivierten Standortdiensten und erfassen damit nicht notwendigerweise alle Verkehrsteilnehmer repräsentativ. Wenn sich die Zusammensetzung der Baidu-Nutzer über Zeit oder zwischen Städten unterscheidet, kann dies die gemessene Straßengeschwindigkeit beeinflussen.

Hinzu kommt, dass das Sample räumlich begrenzt ist. Die Analyse konzentriert sich auf Straßenabschnitte innerhalb von 2,5-Kilometer-Bufferzonen um ausgewählte U-Bahn-Linien und besonders auf directly affected road segments. Das ist für die Identifikation sinnvoll, weil dort der Effekt am plausibelsten ist. Für die Interpretation bedeutet es aber, dass der geschätzte Effekt nicht automatisch als citywide Effekt gelesen werden kann. Die Studie zeigt also überzeugender, dass U-Bahnen bestimmte betroffene Straßen entlasten, aber weniger eindeutig, wie stark die gesamte städtische Verkehrsbelastung sinkt.

### Externe Validität und langfristige Effekte

Die Ergebnisse gelten vor allem für chinesische Städte während einer Phase massiven U-Bahn-Ausbaus. Diese Städte hatten starkes Bevölkerungswachstum, steigende Autobesitzraten und häufig hohe Anfangscongestion. In anderen Ländern oder in Städten mit bereits sehr dichten ÖPNV-Netzen können die Effekte anders ausfallen. Auch institutionelle Faktoren wie Stadtplanung, Siedlungsdichte, Autonutzungskosten und Qualität des Busnetzes beeinflussen, ob eine neue U-Bahn-Linie tatsächlich Autofahrten ersetzt.

Außerdem misst die Studie vor allem kurz- bis mittelfristige Effekte im ersten Jahr nach Eröffnung. Langfristig könnten sich Haushalte, Unternehmen und Pendler an die neue Infrastruktur anpassen. Eine bessere U-Bahn-Anbindung kann Wohn- und Arbeitsortentscheidungen verändern, neue Nachfrage erzeugen und zusätzliche Mobilität ermöglichen. Aus Sicht des Fundamental Law of Road Congestion ist daher denkbar, dass ein Teil der Entlastung langfristig durch induzierte Nachfrage wieder abgeschwächt wird. Die Studie kann zeigen, dass U-Bahnen kurzfristig Stau reduzieren, aber sie kann nicht abschließend beweisen, dass dieser Effekt dauerhaft in gleicher Größe bestehen bleibt.

### Welfare-Impact-Berechnung

Die Welfare-Berechnung ist nützlich, weil sie die statistischen Ergebnisse ökonomisch interpretierbar macht. Gleichzeitig ist sie deutlich weniger kausal abgesichert als die Hauptschätzung zur Straßengeschwindigkeit. Der am besten identifizierte Bestandteil ist der Zeitgewinn der verbleibenden Straßennutzerinnen und Straßennutzer. Dieser Teil hängt direkt mit dem geschätzten Geschwindigkeitseffekt zusammen. Die weiteren Bestandteile der Wohlfahrtsrechnung, insbesondere die Gewinne der Personen, die zur U-Bahn wechseln, sowie die staatlichen Kosten und Subventionen, beruhen jedoch stärker auf zusätzlichen Annahmen und ergänzenden Daten.

Kritisch ist vor allem, dass Präferenzen, Komfortunterschiede und tatsächliche Opportunitätskosten der Reisezeit nur approximiert werden können. Die Annahme, dass die monetäre Bewertung der Pendelzeit einem bestimmten Anteil des Lohns entspricht, ist üblich, aber nicht zwingend für alle Pendlergruppen korrekt. Auch die Kosten des U-Bahn-Systems sind schwer vollständig zu erfassen, weil Baukosten, Betriebskosten, Subventionen, Kapazitätsauslastung und langfristige Instandhaltung berücksichtigt werden müssten. Wenn diese Größen falsch angesetzt werden, kann sich die Wohlfahrtsbilanz deutlich verändern.

Zudem bleiben mehrere potenzielle Nutzenkomponenten außerhalb der Berechnung oder werden nur indirekt behandelt, etwa Luftverschmutzung, Lärm, Verkehrssicherheit, Flächennutzung und langfristige Agglomerationseffekte. Das bedeutet nicht, dass die Welfare-Analyse falsch ist. Sie sollte aber eher als Back-of-the-envelope-Interpretation verstanden werden und nicht als präzise Kosten-Nutzen-Analyse. Die Kausalität der Geschwindigkeitswirkung ist deutlich stärker belegt als die Aussage, dass der gesamte Wohlfahrtsnutzen des U-Bahn-Systems seine Kosten übersteigt.




# 10 Fazit

Die Replikation bestätigt die zentralen empirischen Ergebnisse von Gu et al. (2021) qualitativ. Die wichtigsten Tabellen und Abbildungen zu den Haupteffekten, Robustheitsanalysen und heterogenen Effekten konnten in R nachgebildet werden. Zusätzlich wurde aus den Informationen zu den Eröffnungszeitpunkten der U-Bahn-Linien eine eigene Grafik erstellt, um das Treatment Timing visuell klarer darzustellen. Diese eigene Darstellung macht besonders deutlich, dass sich viele Eröffnungen am Jahresende und damit in zeitlicher Nähe zu Chinese New Year häufen.

Inhaltlich spricht die Replikation dafür, dass neue U-Bahn-Linien kurzfristig die Straßengeschwindigkeit auf betroffenen Straßenabschnitten erhöhen. Die Hauptergebnisse liegen je nach Spezifikation ungefähr im Bereich von 3,6 bis 4,4 Prozent und bleiben auch in mehreren Robustheitschecks positiv. Besonders überzeugend ist, dass die dynamischen Effekte erst nach der Eröffnung sichtbar werden und dass stärker betroffene, stärker überlastete und näher an der neuen Linie gelegene Straßen größere Effekte zeigen. Dieses Muster passt zur ökonomischen Substitutionslogik: Wenn Pendlerinnen und Pendler vom Auto oder Bus zur U-Bahn wechseln, sollten vor allem jene Straßen entlastet werden, auf denen zuvor hohe Staukosten bestanden.

Trotzdem ist die kausale Interpretation nicht vollständig unangreifbar. Die größte Schwachstelle bleibt das Treatment Timing. Viele Linien wurden in einer Periode eröffnet, in der sich Verkehrsmuster in chinesischen Städten aufgrund von Chinese New Year ohnehin stark verändern. Außerdem unterscheiden sich Treatment- und Kontrollstädte systematisch in Größe und Wohlstand. Die Autoren kontrollieren diese Probleme mit Fixed Effects, Saisonalitätsanpassungen, Placebo-Tests und Teilstichprobenanalysen. Diese Strategien machen die Ergebnisse plausibler, können aber nicht vollständig beweisen, dass alle zeitgleichen saisonalen oder administrativen Effekte ausgeschlossen sind.

Auch die ökonomische Interpretation sollte vorsichtig gelesen werden. Der geschätzte Geschwindigkeitseffekt ist relativ gut identifiziert, aber die daraus abgeleitete Welfare-Berechnung beruht auf zusätzlichen Annahmen über Zeitwerte, Pendelverhalten, Kosten des U-Bahn-Systems und Verhaltensanpassungen. Deshalb ist die Aussage, dass U-Bahn-Ausbau Straßen kurzfristig entlastet, deutlich stärker abgesichert als die weitergehende Aussage, dass der gesamte Wohlfahrtsnutzen die Kosten des Systems übersteigt.

Ein idealer Datensatz für zukünftige Forschung würde zufälligere oder zumindest weniger saisonal gebündelte Eröffnungszeitpunkte enthalten, Treatment- und Kontrollstädte besser vergleichbar machen und den gesamten Stadtverkehr statt nur ausgewählter Bufferzonen abbilden. Zusätzlich wären langfristige Daten über mehrere Jahre hilfreich, um zu prüfen, ob die Entlastung dauerhaft bestehen bleibt oder durch induzierte Nachfrage, Umzüge, neue Pendelmuster und steigenden Autobesitz wieder abgeschwächt wird. Insgesamt sind die Ergebnisse damit plausibel und empirisch gut dokumentiert, aber nicht vollständig frei von Identifikationsrisiken.

# 11 Anhang

### Paketliste

Für die Replikation und Darstellung der Ergebnisse wurden folgende R-Pakete verwendet:


- haven
- dplyr
- ggplot2
- tidyr
- grid
- lubridate
- knitr
- kableExtra
- fixest
- broom
- purrr
- patchwork
- readr


Die Pakete erfüllen unterschiedliche Funktionen innerhalb des Reports. `haven` wurde zum Einlesen der Stata-Datensätze verwendet. Die Datenaufbereitung erfolgte vor allem mit `dplyr`, `tidyr`, `lubridate`, `purrr` und `readr`. Für die Regressionsschätzungen wurde `fixest` genutzt, da damit Fixed-Effects-Modelle effizient geschätzt werden können. Die grafische Darstellung erfolgte überwiegend mit `ggplot2`, `grid` und `patchwork`. Tabellen wurden mit `knitr` und `kableExtra` erstellt und formatiert; teilweise wurde zusätzlich CSS eingesetzt, um die Tabellen und Abbildungen optisch näher an die Darstellung im Paper anzupassen. Das war vor allem dort notwendig, wo eine rein standardisierte R-Ausgabe nicht ausreichte, um ein gut lesbares und mit dem Original vergleichbares HTML-Ergebnis zu erzeugen.

### Reproduzierbarkeit

Die Replikation basiert auf den vom Paper bereitgestellten Replikationsdateien. Die ursprünglichen Autoren stellen Datensätze, Stata-Code, Log-Dateien und Ergebnisdateien bereit. Der Originalcode ist in Stata geschrieben; für diesen Report wurden die relevanten Analysen in R nachvollzogen.

Die wichtigsten empirischen Bestandteile des Papers konnten im Rahmen dieser Arbeit repliziert oder nachgebildet werden. Dazu gehören die zentralen deskriptiven Tabellen, die dynamischen Event-Study-Grafiken, die Baseline-Regressionen, mehrere Robustheitsanalysen sowie die heterogenen Effekte nach Straßenmerkmalen. Für Table 1 wurde keine reine Tabellenreplikation erstellt. Stattdessen wurde aus den Informationen zu den U-Bahn-Eröffnungen eine eigene Grafik entwickelt, die das Treatment Timing und insbesondere die Häufung der Eröffnungen rund um Chinese New Year besser sichtbar macht. Figure 7 ist keine empirische Schätzung, sondern eine konzeptionelle Wohlfahrtsgrafik; sie wurde daher nicht als Regressionsergebnis repliziert, sondern für die ökonomische Interpretation grafisch nachgebildet beziehungsweise erläutert.

Nicht vollständig reproduzierbar war Table 6. Laut README beruhen die dafür notwendigen Datensätze `IndModeSample.dta` und `HhdVKTSample.dta` auf restricted-use Microdaten der Beijing Household Travel Surveys und sind nicht im Replication Packet enthalten. Table 6 kann deshalb mit den öffentlich bereitgestellten Replikationsdateien nicht vollständig repliziert werden.

Ebenfalls eingeschränkt reproduzierbar sind Teile des ursprünglichen Data Cleaning. Die README verweist darauf, dass zwar Beispiel-Rohdaten und Cleaning-Codes bereitgestellt werden, die vollständigen Original-Rohdaten aber nicht vollständig im Replication Packet enthalten sind. Die vorliegende Replikation setzt daher auf den bereitgestellten Analyse-Datensätzen auf. Das bedeutet: Die Regressions- und Grafikoutputs sind aus den verfügbaren Replikationsdaten nachvollziehbar, aber die vollständige Erzeugung aller Analyse-Datensätze aus den proprietären Rohdaten ist nicht Teil dieser Replikation.


```{r session-info, echo=FALSE}
sessionInfo()
```







































